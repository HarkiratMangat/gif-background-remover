# GIF Background Remover — deferred list

The project-local tracker for flagged findings, real TODOs, and reminders specific to this repo — split out from `/Applications/Claude Code/meta-deferred-list.md` on 2026-08-07, the same treatment Dior's Builds got (`docs/db-deferred-list.md`). That file stays the canonical home for things with no single-project home; this one is for anything a session working *only* in this repo would need.

⚠️ **HISTORICAL as of 2026-08-21 — this ordering is SPENT: Parts 1, 2 and 4 all shipped in v6.0.0 and Part 3 is still deferred. The live queue is the open items below, headed by the three-tier agent trial. Kept because the reasoning behind the sequencing is still the record of why Part 3 waits.** ORDER OF WORK, set 2026-08-21 by Harkirat, superseded the 2026-08-20 ordering below it: Part 2 next session (the erosion exemption, the 40-frame-spread first slice, and the soft-glow-on-dark-background item — read together, see the erosion item and AUTONOMY BACKLOG item 5 for why), then Part 3 stays deferred (`CatPackFree`, the keyer pair), then Part 4 (`docs/plans/2026-08-21-part4-agent-overhead-reduction.md` — reducing agent tool-call overhead, not script runtime; do not start it before Part 2).** ⚠️ **Part 4 gained a Task D on 2026-08-21: mine Dior's Builds' parallelism/caching implementation** (which Harkirat notes was crafted by looking at this repo's initial version) for what transfers to three separate consumers — the TESTING workflow first (re-tests and cold tests are the stated pain; the open question is whether they solved dependency-scoped cache invalidation, since ours is keyed on the script SHA and any product edit invalidates everything), the general repo workflow, and the packaged `.skill` itself. **Filed only — explicitly NOT to be investigated before the Part 4 session.** ⚠️ **Part 4's handoff is `docs/handoffs/2026-08-21-part4-agent-overhead.md` (written 2026-08-21); every OTHER file in `docs/handoffs/` is stale.** Part 2 is complete — the erosion exemption, the earned erosion-2 ceiling, `--edge-softening`, frame nomination, an `--analyze` crash fix and the soft-glow candidate all shipped into the unreleased v6.0.0. ⚠️ **Part 4 is COMPLETE (2026-08-21), all four tasks — and it shipped: `feat/v6-backlog` was squash-merged as `7bceb9f`, tagged `v6.0.0` and published as the Latest GitHub Release, with the built `.skill` attached.** Task A: the CLI takes N files in one invocation (`--out-dir` for derived names), and SKILL.md states the rule. Task B: the ask-first size/format gate, plus `--target-kb`'s WebP/AVIF/APNG search rebuilt as a concurrent 120-rung grid with **deep downscale demoted below frame-stride** — Harkirat's call, on a measured galaxy rung table where downscaling to 75% made the file 2.3x LARGER (`references/lessons.md` §42). Task C: struck, Part 2 shipped it. Task D: Dior's Builds **did** solve dependency-scoped cache invalidation; adopted as `analysis_fingerprint`, measured to keep the analysis cache warm across **32.1% of real product commits** where the whole-file key kept none — write-up at `docs/investigations/2026-08-21-diors-builds-caching-transfer.md`. Read the plan's own Outcome table before re-reading its task list; two of its premises were falsified by carrying it out. ⚠️ **`docs/handoffs/2026-08-20-v6-backlog-session.md` is now STALE and should NOT be read as current** — it frames `Cut loop.gif` as a confirmed non-ring outlier needing a new exemption mechanism, which was retracted 2026-08-21 (the numbers were a `score_outputs` frame-coalescing bug; re-scored correctly, `Cut loop.gif` is an ordinary member of the ring population, not an outlier — see the erosion item below and `references/lessons.md` §37.6, §37.8). This file's own items are the current source of truth; a session should read them directly rather than through that handoff. ⚠️ **`docs/plans/2026-08-20-remaining-work-ordered-by-impact.md` has also been EXECUTED** — read its outcome table, not its task list: four of its seven items are closed, and two of its own assertions were falsified by carrying them out. It is history now, not a queue.

⚠️ **The seven items filed 2026-08-19 came from a three-agent trial on real assets, not from an audit.** Read `docs/investigations/2026-08-19-three-agent-package-trial.md` before working any of them — it explains why they cluster where they do, and `docs/plans/2026-08-20-post-trial-defects.md` sequences them (the scorer first, because it currently certifies the worst defect as fixed).

**This file holds OPEN work only.** Closed items live in `gif-resolved-list.md` — see the conservation rule below. Restructured 2026-08-18 (Harkirat's call) after the tracker was found reporting 14 open items when 12 was the truth: open and closed items were interleaved under a section titled "Resolved", three `###` headings read as open above bodies that said `✅ CLOSED`, and a live P1 was two screens below a closed one. A tracker a session has to *audit* before it can *use* is not serving as a roadmap.

**The conservation rule — one item removed here MUST equal one item added to `gif-resolved-list.md`.** Never delete an item; move it, keeping its original wording and adding the outcome. This is gated, not trusted: `python3 scripts/audit_docs.py --diff <base>` fails if this file loses a substantive line that cannot be traced into the archive by content. The gate exists because "tidying" and "deleting" look identical in a diff, and because this repo has already shipped a tracker that lied about its own contents.

**Priority & effort tags** (short copy — canonical legend + full model/effort grid at `meta-deferred-list.md`, section "🏷️ Priority & effort tags"): every open item carries `[Priority · Effort · Model-effort]`.
- **Priority:** P0 now (broken/blocking) · P1 soon · P2 eventually (real, not pressing) · P3 someday.
- **Effort:** XS minutes · S part of a session · M a session · L its own multi-session job (must carry a named first slice — no schedulable "just L" items).
- **Model+effort:** chosen from the priority-tier grid (premise risk × deliberation load), not from the effort tier — see the meta file for the full grid.

---

## 🐞 Open — real TODOs with an available fix, not yet done

### SESSION-REPORT FINDINGS — three claude.ai bundles re-audited against v6.4.1 `[P1 · L (first slice: items 1 + 2, S) · Opus5-High]` *(filed 2026-09-09)*
**Read `docs/specs/2026-09-09-session-report-findings.md` before working any of these.** It carries the reproductions, acceptance tests, an evidence class per finding, **four** retracted claims and the dependency order. Bundles live in `local/claude ai session reports/` and are gitignored — a fresh clone has the spec, the fixture and the investigation table, and none of the rest. The nine items below group **thirteen findings**: four reproduced by running the tool or measuring real assets, one observed visually, five established by code read only, and four open hypotheses each carrying a named test. ⚠️ **The spec was rewritten after a three-agent review falsified its central acceptance claim** — work from the current file, not from any recollection of the first draft.

1. **`recommend()` returns `gif-ok` on a 70%-partial-alpha source** `[P1 · S · Sonnet-high]` — reproduced on v6.4.1. Following `suggested_command` takes 2,202,940 partial-alpha px to **0** while `a>0` retention reads 100% and stderr prints `durations preserved exactly`. The verdict comes from `detect_fading_colors` (`:5629`) over the art PALETTE, which never reads the alpha plane. **`_partial_alpha_seen` already exists at `:1794` and is exposed at `:2090`; the verdict block at `:2924-2974` never consults it.** Gate on a FRACTION, not a count. Acceptance: this asset goes non-`gif-ok`; several opaque GIF controls stay unchanged.
2. **`source_has_pre_existing_transparency` uses the palette-index spelling** `[P1 · XS · Sonnet-medium]` — `:2200` reads `'transparency' in im.info`, reporting `false` on a file with 315,504 alpha-0 px, contradicting `background_color_stability` in the same JSON. `get_source_transparency_mask` (`:943`) was fixed for exactly this in August; the report field was not. Its own comment says this project *keeps hitting* the inverse-spelling failure — **grep every other `im.info`-derived report field in the same pass.**
3. **`detect_bg_color` samples four corner pixels** `[P2 · S · Sonnet-high]` — measured over 101 corpus images: **3** have four agreeing corners; **38** give an answer further than `--tolerance` from the true border mode, up to 38.7. Table: `docs/investigations/2026-09-09-devoid-corpus-bg-detection.md`. Not yet shown to break a delivered asset — an exposure with a thin margin.
4. **Background as a FIELD, not a colour** `[P2 · L · Opus-high]` — first slice: the estimator plus its refusal path and null control, no consumer changes. A single colour cannot express a vignette: measured background spread **79.7** on the app icon against `--tolerance 15`; keying it needs ~80, which puts **12.14% of the artwork** inside the key. The discipline session 2 arrived at for its own v6 build is the build spec (anchor outside the feature; refuse where there is no data; prove non-flatness before correcting; null-control on a flat asset; gate per region).
5. **The erosion calibration curve's FLAT case is computed and discarded** `[P2 · XS · Sonnet-medium]` — `calibrate_edge_cleanup_erosion` reads CONVEXITY (the knee at 2, §37.10) but never flatness. Session 1 measured the tell on the wordmark: 0.6349 → 0.5862, "nothing trimmable". Statistic is `v0 − min(v)`, **not** variance across all four. The table already exists in memory. Cheapest item on this list.
6. **`recommend()` can only move the band's ENDPOINT** `[P2 · S · Sonnet-high]` — it appends `--feather-band-multiplier` (`:2852`) and never a `--tolerance` value. Session 1's accepted command moved the START (15→8); session 3 measured that the start is the perceptual variable (0.34-0.475 reads as "fading", 0.66 as "a crop", identical endpoint). ⚠️ **Fixing this MOVES PIXELS** — `auto_run` splits `suggested_command`'s flags at `:9967` and applies any the caller left at default (`:9976-9992`), which is how `--feather-band-multiplier` already reaches real conversions. It is not a reporting change; it needs its own constructed acceptance test, and a `render_baseline --compare` of `0 changed` would not prove it safe.
7. **No absolute check anywhere** `[P2 · XS · Sonnet-medium]` — every gate is differential, so a uniform error on the whole alpha plane cancels in all of them. Session 3 shipped a global 5% opacity ceiling nobody noticed. One assertion: on a source with fully opaque artwork, the output must contain alpha-255 pixels.
8. **`--edge-fade <start>[,<p>]`** `[P2 · M · Sonnet-high]` — no flag builds a spatial alpha map, which is why a 15-turn job happened outside the tool. Session 3 supplies a reference implementation, converged defaults (`start=0.45, p=4.0`) and a bit-exact acceptance test. Apply as `minimum`, never assignment (§31); decline on `.gif` with the reason printed.
9. **Four open hypotheses, each with its test** `[P2 · M · Opus-high]` — (a) the tool composites in sRGB with no gamma handling anywhere — ⚠️ **the test named in the first draft was unexecutable**: no "labelled GIF corpus" exists, and labels encode the wrong property; the spec now restates it as a selection on flat-background antialiased art with a pass/fail rule. Then, which is correct for sRGB-authored art and wrong for linear-authored art (AI generators); **two attempts to confirm on real content failed** — run the bow test on the labelled GIF corpus, where flat backgrounds are the norm. (b) `a_safe = clip(alpha, 0.25, 1.0)` binds on **28.2%** of real band pixels; effect unknown. (c) the fit ladder descends to 0.25 scale with nothing measuring small-feature survival. (d) pooling hardness across frames should make a tumbling hard-edged sprite misclassify as antialiased.

⚠️ **Three claims from this audit were RETRACTED and must not be re-derived** — the clamp's direction, the gamma bow "confirmation", and treating session 3's agreement with sessions 1 and 2 as independent evidence (it read both first). §6 of the report carries each with its cause.


### `[P2 · XS · Sonnet5-Med]` The unpackaged-pointer check cannot see an extensionless filename *(filed 2026-09-07 16:43 EDT)*

`audit_docs.py`'s rule that a packaged file must never point at a repo file the `.skill` does not contain works on names **with an extension**. Falsified in both directions 2026-09-07 16:43 EDT: a `SKILL.md` pointer at `gif-deferred-list.md` fails the gate correctly, and a pointer at `COPYING` passes it **even with `COPYING` removed from the `packaged` set** — the matcher never considers the string a filename.

Nothing is broken today: `COPYING` and `COPYING.LESSER` really are packaged. But the class is real, and it is the same shape as every other finding in this repo's history — **a gate that reports success on a case it cannot see.** Any future packaged pointer at `LICENSE`, `Makefile`, `Dockerfile` or a bare directory name would be waved through.

**Concrete next action:** widen the matcher to catch backticked strings that name a real file at the repo root regardless of extension, then re-run the two falsifiers above and confirm the `COPYING` case now needs the `packaged` entry to pass.

### `[P1 · S · Opus5-High]` The conservation gate WARNS where two files say it FAILS *(filed 2026-09-07 14:17 EDT)*

`gif-deferred-list.md` and `gif-resolved-list.md` both state the rule the same way: *"`python3 scripts/audit_docs.py --diff <base>` **fails** a branch that removes a substantive line from the active list without adding traceable text here."* It does not fail. Falsified 2026-09-07 14:17 EDT: an 11-line open item was deleted, archived nowhere, and committed; the gate printed `WARN: 5 of 12 line(s) removed ... could not be traced`, then `all doc gates pass (1 warning(s))` and **exited 0**.

⚠️ **Two separate problems, and the second is worse than the first.** (1) A warning inside a passing run blocks nothing — this repo has already been bitten by exactly that shape in `reflow-prose.mjs`, whose own comment records `--check` exiting 0 on its only real finding while a hard-wrapped file sat under it. (2) The tracing is loose: of 12 removed lines it called **7 traceable** for an item that was never archived at all, so even the warning under-reports.

**Concrete next action:** decide which the rule is — a gate or an advisory — and make one of the three files match the other two. If it is a gate, `--diff` exits 1 on any untraceable removal and the loose matcher is tightened (an exact-substring hit on a stripped-through line, not a fuzzy one). ⚠️ **The gate is blind to the working tree either way**: it diffs commits, so an uncommitted deletion is invisible. That is defensible for a pre-merge check and must be stated, because a session running it before committing gets a clean pass on a broken tree.

⚠️ Unrelated, found in the same pass and not worth its own item: `docs/plans/2026-08-22-target-kb-constraints-and-format-ranking.md` fails the soft-wrap check on `main` (1573 → 1551 lines). One `--write` fixes it.


### `[P1 · S · Opus5-High]` Two scorer tests fail on `main`, and the one that fails is the one that exists to catch the worst defect *(filed 2026-09-07 14:15 EDT)*

`python3 -m pytest scripts/harness` on `main` exits 1 with two failures in `scripts/harness/test_score_outputs.py`:

```
test_worst_frame_not_mean                        bg_removed_worst 1.0000, expected < 0.98
test_a_solid_background_wedge_is_caught_by_opacity  bg_not_opaque_worst 1.0000, expected < 0.99
```

Both score `growth.gif` against `agent-3-expert/growth_transparent.webp`, the asset whose docstring says it *"holds an opaque background wedge on 16 consecutive frames"*. **The scorer now reports that file as perfect on both measures.** ⚠️ Confirmed PRE-EXISTING, not a branch effect: reproduced with `main`'s `scripts/remove_gif_background.py` swapped into the branch checkout, same two assertions, same numbers.

Two possibilities and they are not equally bad. Either the fixture under `local/` was regenerated and no longer holds the wedge — in which case the test lost its subject and must be repointed at an asset that still fails — or the scorer regressed and is certifying a known defect as fixed. **That second one is `docs/plans/2026-08-20-post-trial-defects.md` Task 1's exact failure mode**, the reason the scorer was rebuilt: a measure that returned 1.000 on all seven trial outputs *including the broken one*. It is filed P1 for that reason alone.

**Concrete next action:** open the fixture and count its opaque background pixels per frame before touching the scorer. Which of the two it is, is a measurement, and the cheaper one comes first.

### `[P2 · S · Sonnet5-High]` Nothing collects labels for the protection decision, and the harness README used to say something did *(filed 2026-09-07 14:15 EDT)*

`scripts/harness/labels/` holds **981 `edge_hardness` judgements and zero protection judgements** — for the coin-flip `--auto` refuses on, which is **10.2%** of assets (31 of 304) on the enclosure question alone. Its README pointed at the Devoid app as the collector; that writer was removed on 2026-09-07 at that repo owner's instruction and the pointer is now corrected. ⚠️ **This repo believed for three days that the harder question was being labelled.**

The consumer of these labels is this repo, so the collector is this repo's decision. **Concrete next action:** decide whether the enclosure verdict is worth labelling at all before building anything — `references/lessons.md` already carries the measured refusal rates, and a labelled corpus is only worth its collection cost if a mechanism would be tuned against it. If yes, the schema Devoid froze (`ts`, `asset_id`, `outline_color`, `enclosure_ratio`, `frames_enclosed`, `frames_checked`, `bbox_xyxy`, `verdict`) is a working starting point and is recorded in that repo's `docs/API-CONTRACT.md`.


*Ordered by priority, P1 first. Every item here is genuinely open: if you find a `✅ CLOSED` marker in a body under this heading, that is the drift `audit_docs.py`'s tracker gate exists to catch — report it rather than trusting either half.*

### `[P0 · L (first slice: Task 9) · Opus5-Med]` v6.0.0 output defects — a 1px alpha fringe on every WebP/AVIF, plus nine more *(filed 2026-08-22, from the timeout trial + Harkirat's own claude.ai session + his review of the delivered files)*

**Two claude.ai sessions timed out on a two-GIF job. Reproducing it found ten defects; asking Harkirat to look at the output found an eleventh that all ten measurements had missed.**

📄 **Findings and evidence:** `docs/investigations/2026-08-22-v6-timeout-trial.md` (13 findings, severity derived from a stated rule, plus a section on what nobody checked). 📄 **Implementation plan:** `docs/plans/2026-08-22-target-kb-constraints-and-format-ranking.md` (10 TDD tasks, ordered by value, with a gate and two open design questions at the top). 📁 **Raw artifacts:** `local/2026-08-21-v6-timeout-trial/` — the subagent trial's timeline and nine numbered command logs, Harkirat's full claude.ai session record, contact sheets of the delivered files, and the rejected 120px deliverable kept as evidence.

**Do Task 9 first and treat it as its own slice.** Edge-cleanup erosion defaults to **0** for every 8-bit-alpha format (`scripts/remove_gif_background.py:7193`, "8-bit alpha needs no fringe trim"), leaving a ~1px light fringe that Harkirat named on sight — while `--verify`'s `edge_fringe_check` reported that same file `looks_fringed: false` at 0.0000. Measured on a controlled pair, same asset and flags: erosion 0 leaves **624 partial-alpha halo pixels** and an outer ring 16.9% closer to white; erosion 1 leaves **0**. `--auto` calibrates and gets it right; the manual path never calibrates. ⛔ **Do not simply flip the default to 1** — 448 renders' worth of evidence says erosion eats thin art. The plan's preferred fix is to run the existing calibration unconditionally, gated on a PRE/POST render diff where a non-zero diff is expected and `art_kept_worst` must never fall.

**The other high-severity items**, each with a task in the plan: `--auto` protects the sparkles a user explicitly asked to remove and reports success (Task 10 — Harkirat chose "ask", with `--assume-protect`/`--assume-remove` so an unattended run can pre-answer); `--target-kb` has no minimum-dimension concept and silently shipped a 120px-wide file against a stated 128px floor (Task 1, and the fix must reach `--resize-max-dim` and the compress tiers too, not only the fit); the final dimensions are never reprinted after a fit so the last printed `Output:` line is stale, which is what made the claude.ai session report 482x513 for a 120x128 file (Task 7, ~3 lines); `SKILL.md`'s navigation recipe is `rg`-only and its `grep` substitute returns **0 matches at exit 0** (Task 8, alongside moving 28.9% of the file — the release notes — into `references/version-history.md`); `--webp-quality` is silently discarded unless `--webp-lossy` is passed (Task 6).

⚠️ **Runtime is the reason the sessions died, and it is only partly fixable.** `--target-kb` is a 120-rung grid where each rung re-encodes every frame: 207.84s wall / 443s CPU for one 144-frame asset here, and the claude.ai sandbox has **one logical CPU**, so it runs serially and cannot finish inside a tool call. `--verify` on the same asset also exceeded 2 minutes. **A background job is NOT the workaround** — `nohup ... &` there returns a PID and the next tool call finds an empty log and no process; each tool call is torn down with its own process group. Task 3 adds a pre-flight estimate and SKILL.md gains the "split the call" exception.

⚠️ **One hypothesis was tested and FALSIFIED, do not re-derive it:** the WebP ladder's q60 floor did **not** cause the resolution collapse — full-res WebP is 1730 KB at q45, ~7x over cap. What it revealed instead is that **AVIF is 7.7x smaller than WebP at equal nominal quality** on 144-frame alpha content, which is why a strictly-dominated WebP keeps shipping next to a better AVIF (Task 2).

**Two things deliberately NOT tasks, filed here so they are not silently dropped:**
1. **Frame-stride weighting.** Harkirat finds the stride 3-4 output "visually detectable" on full playback, but `_STRIDE_COST` was set on a real `galaxy.gif` byte measurement. Changing weights on one reaction is the [[feedback_do_not_relabel_when_a_measure_objects]] mistake. This needs its own measurement of what a viewer notices at stride 2/3/4 against what a deep downscale costs.
2. **The "downscaling made this LARGER" diagnostic** is printed correctly and then ignored by the search that printed it. Low severity, and fixing it touches the rung ordering the plan is forbidden to move.

**Still unanswered, and the plan gates two tasks on them:** what the min-dimension DEFAULT should be when no axis is named, and whether a pre-answered unattended `--auto` run should proceed silently or log that it acted on an assumption.

⚠️ **The process lesson this item exists to preserve.** Ten findings came from `--verify` JSON, corpus reasoning, code reading, a subagent trial and a full session record. The eleventh — the one Harkirat cared about most — came from showing him six composited frames and asking what he'd change. Every automatic regime here compares the product to itself or to a label; none can see whether the result is *wanted*. Render the ACTUAL delivered files, not uncompressed intermediates: both sessions verified against intermediates and both missed the fringe.

### `[P0 · M · Opus5-Med]` The tool cannot express "this enclosed interior is background", and `--fade-color` does not recover the fade it names *(filed 2026-08-22, assets from Harkirat, corrected the same day after he reviewed the renders)*

📄 `docs/investigations/2026-08-22-v6-timeout-trial.md` §13A (findings table at §15, open questions at §16) · 📄 `docs/plans/2026-08-22-target-kb-constraints-and-format-ranking.md` — **read its STATUS block first** · 📁 `local/2026-08-22-fade-edge-cases/`

⚠️ **THIS ITEM AND THE P0 ABOVE IT ARE ONE BODY OF WORK AND FOLD INTO ONE SKILL-IMPROVEMENT SESSION.** The plan is the queue: 16 tasks, two shipped in v6.1.0 (`--unprotect-region` and the recommender offering it), one DONE (Task 8, SKILL.md debloat + portable navigation — header is now 8.4% of the file, both the version-block move and the grep-not-rg navigation fix are shipped and covered by `scripts/harness/test_skill_navigation_is_portable.py`; verified 2026-08-24 against the current file, not previously marked closed here), the rest open with a stated order and two design questions gating Tasks 1 and 2.

⚠️ **RETRACTED 2026-08-23 — the paragraph below is wrong about the MECHANISM, and the correction is the useful part.** `--fade-color` was never broken; it is read only inside the `--recover-fade-alpha` branch, so passed alone it was parsed and thrown away. Measured on notification.gif frame 14: `--recover-fade-alpha --fade-color fd6050` leaves the faint stages **41.5% opaque, mean alpha 0.522**, while `--fade-color fd6050` ALONE leaves them **0.0% opaque, mean alpha 0.000** — removed outright, not "left opaque". The 90.9%-opaque reading came from the `--protect-outline-color f05050,002864` in the same command, which was holding those pixels opaque while `--fade-color` did nothing. **Two mechanisms were read as one bug because only the end state was looked at.** Shipped as a hard error naming the pair; `references/lessons.md` §44.6. The original text follows, kept because the reasoning error is worth being able to find.

⚠️ **`--fade-color` DOES NOT WORK on `notification.gif`, and the first draft of this item said it did.** Traced pixel-by-pixel: on frame 14, of the 2,662 source pixels at 5–35% opacity on the `fd6050` → white ray, **92.5% render fully opaque under `--auto` and 90.9% under `--fade-color fd6050`** — the flag the tool's own evidence prescribes moves it by 1.6 points. The earlier "+27% partial alpha" figure was whole-canvas antialiasing rim and never touched the fade; 0.16% mid-alpha against broadcast's working 4.25% was the tell and was not read. **A second defect in the same evidence string:** it predicts those pixels are *"removed as background"* when only **1.8%** reach alpha 0 — they are kept opaque. Task 13 diagnoses this and **BLOCKS Task 12**; do not build a user prompt around a flag that does not function. ⛔ Do not attribute this to `references/lessons.md` §41 — that explains the auto-apply refusal, not the manual path's failure.

⚠️ **The "either/or" on `broadcast.gif` is REAL in the recommender and NOT in the tool — an earlier filing called it a capability gap and that is RETRACTED.** `--remove-region` is applied at `scripts/remove_gif_background.py:7729`, downstream of the fade path at `:7346`, so it composes. Measured: `--recover-fade-alpha --erosion-exempt-transient --remove-region "rect:238,332,168,206"` gives **enclosed near-white opaque 8,569 → 0 with the fade intact (4.22% mid-alpha against the fade-only 4.25%)**. What is broken is that `--recommend` emits the mutually exclusive `--protect-outline-color` + `--recover-fade-alpha` pair (Task 11), and its warning — *"Pick one: fade recovery, or region protection"* — reads as a capability statement and steers users away from the answer (Task 14).

**The unifying root, visible only with all three assets side by side: there is no way to tell the tool that an enclosed region of background colour is background.** Explicit outline protection, the fade path's topological protection, and the band-interior scan all independently classify it as intentional design — measured identical on broadcast (8,569 px in both branches, same bboxes). `--remove-region` is the single escape hatch, it composes with everything, and **nothing recommends it**. That one missing suggestion is the root of broadcast's tower, megaphone's sparkles (§13A.3, `--auto` prints `applying: --protect-outline-color f0c850,002864`), and the coin-flip class in general. ✅ **Task 14 is DONE (2026-08-22): `--unprotect-region` is implemented and measured** — broadcast enclosed white 8,569 → 0 with navy artwork 23,240 → 23,631 and the fade intact at 4.48% mid-alpha, against the `--remove-region` attempt's 6,297 navy. Four falsifiers in `scripts/harness/test_unprotect_region.py`, each asserting on what STAYED as well as what left. ⚠️ **Two implementation defects were found afterwards by Harkirat looking at the render, and both are fixed:** a hard colour threshold left 262 px of white on 3 of 60 frames and a shimmering AA rim, and replacing it with a colour ramp alone then held **12.4% of the antialiasing ring fully OPAQUE and pale** — worse. The working form is **colour ramp + the geometric taper `apply_remove_regions` already provides**: colour decides what is background, geometry softens the boundary. Final: ring opaque 0.5%, white residue 0, artwork 23,478. A temporal-stabilisation variant was built and **rejected on measurement** (lag-1 flip rate 0.062 = smooth animation, not noise) — see `references/lessons.md` §43 before rebuilding it. ✅ **Task 14b is DONE too (2026-08-22): `--recommend` now offers `--unprotect-region`** with a bbox-derived `rect:` on any region whose outline does not enclose it on every frame — broadcast derives `rect:243,311,153,197` against a hand-measured `238,300,168,240`. Offered, never applied; `secure.gif` at enclosure 1.000 gets no hint, and that negative is a falsifier. ⚠️ **The 14b hint's box is one frame's extent and leaves 2,340 px of residue when rendered — it is labelled a starting point rather than an answer, and Task 14c derives a real across-frames region.** A padding constant was built and rejected on measurement: +15% clears the residue and costs 1,287 px of artwork by reaching the outer silhouette, and even the exact across-frames extent still leaves 1,607 px because the residue is scattered small blobs a rectangle cannot describe. **This item's remaining work is now Task 14c ALONE.** Tasks 11, 13 and 12 all shipped on `docs/fade-edge-cases` 2026-08-23 — the recommender no longer emits the exclusive pair and publishes the fade-first command as `alternative_command`; `--fade-color` without `--recover-fade-alpha` is a hard error (and the diagnosis CORRECTED this item's own claim — see the retraction below); `--auto` asks about a nameable fade instead of cutting it silently. Formerly: Task 14b — nothing recommends the new flag, so an autonomous run still cannot reach it, and that is the same gap behind megaphone's sparkles.

⚠️ **Process note worth keeping.** Every claim in the first draft of this item came from whole-canvas pixel counts and was plausible. Harkirat looked at two contact strips and overturned two of them in one sentence. **Compare a metric against what a WORKING case scores before calling a delta a fix** — the working number was in the same table.

### `[P0 · M · Opus5-High]` `--recommend` cannot reach the flags that solve "same colour, opposite treatment" — measured on grenade.gif *(filed 2026-09-01, from the four-asset review that produced `references/lessons.md` §48)*
**This is the sharpest autonomy gap the review found, and it is a REACHABILITY gap, not a missing capability.** `grenade.gif`'s fuse-box highlight bar runs from pale lavender to **pure `#ffffff`, bit-identical to the background** — design separable from background only by topology. `--recommend` suggests `--protect-band-only 4`, which by construction protects a band around the removable core and cannot keep a core that IS the background colour.

Measured 2026-09-01, white-bar pixels lost on the worst frame (the bar is present on 42 of 62 frames):

| run | bar px lost | ring-hole residual |
|---|---|---|
| `--recommend`'s `suggested_command` + the user's erosion request | **2,724** | 0 |
| `--auto` | **2,724**, and its own post-render verify printed `leftover background (worst frame): 0` and declared success | 0 |
| `--tumble-safe --keep-bg-blob-if-near ff00ff --hole-size-range 690,740 --hole-max-aspect 1.3 --protect-band-only 4 --feather-band-multiplier 3.3` | **0** | 0 |

**Why the recommender cannot get there:** `--tumble-safe` is appended only when `tumble_risk.likely_tumble_risk` is true — a foreground/background size-MARGIN test, measuring 542.07x on this asset, nowhere near its 3x trigger. That gate is about a tumbling silhouette; §14's use of the same flag is about HOLE DISAMBIGUATION, an unrelated question wearing the same flag. And `--keep-bg-blob-if-near`, `--hole-size-range` and `--hole-max-aspect` appear in **no `flags.append` anywhere in `recommend()`** — enumerated, the only four flags it can append are `--erosion-exempt-transient`, `--pixel-art`, `--recover-fade-alpha` and `--tumble-safe`.

**Concrete next action:** find a discriminator for "an enclosed interior whose core is EXACTLY the background colour while a same-colour region elsewhere must be removed", then let `recommend()` emit the §14 combination with a measured size/aspect gate. ⚠️ **Do not close this by widening `is_intentional_design`'s narrow-miss band** — grenade's region 4 sits at 0.725 enclosure / 2.0% canvas against bars of 0.9 and 0.5@2.5%, and moving either to fit one asset is exactly the failure `feedback_do_not_relabel_when_a_measure_objects` names. Any change here alters the `--auto` refusal rate (12.8% across 304 assets today) and needs that re-measured, which is why it was NOT done in the session that found it.

⚠️ **Second, related defect in the same asset: `verify()` has no term for design deleted by a protection flag that never covered it.** `--auto` reported `leftover background (worst frame): 0` over a render missing 2,724 px of white artwork on 42 frames. Same shape as §37's "the fringe metric has no term for what erosion costs". A candidate check: for every region `analyze()` saw as background-coloured-but-enclosed on SOME frames, report how many of its pixels went transparent — and say so, rather than only reporting the regions it labelled design.

### `[P1 · M · Opus5-High]` Candidate regions are grouped by canvas proximity, not per-frame connectivity — one region id held a hole to punch AND decoration to keep *(filed 2026-09-01, `references/lessons.md` §48.7)*
On `marketing-automation.gif`, the gear pinhole (1,641-3,727 px, must be removed) and the megaphone's white highlight stripe (~5,500-5,730 px, must be kept) are **two separate connected components in every one of 171 frames**, and `--recommend` returned them as a single "region 4", bbox `(287,200)-(468,484)`, because candidate regions are grouped by canvas-position proximity across the whole animation.

Every downstream failure follows from that one merge, all measured 2026-09-01:
- the `--unprotect-region rect:287,200,181,284` box the hint offers covers **both**, so following it verbatim deletes the stripe;
- `--auto --assume-remove 002864` answers the merged region and destroys the stripe — **5,777 px transparent on the worst frame**;
- a `--remove-region-track` seed taken from the region bbox would seed on the union rather than on the hole.

**Interim mitigation shipped in v6.3.0:** the hint now says to seed the COMPONENT rather than the region box, and names the grouping as the reason. **The root fix is to split a candidate region by real per-frame connectivity**, which changes `analyze()` output for every asset and therefore needs a corpus re-measure — deliberately not attempted in the session that found it, per Harkirat's instruction to keep that session scoped to the four assets.

⚠️ **The correct answer for this asset today is one command, and it works** — `--recover-fade-alpha --fade-color 6964f8 --remove-region-track "rect:437,301,32,67"` gives 0 residual opaque px in the hole and 0 gaps in the stripe across all 171 frames with the fade intact. The gap is that nothing derives that seed for you.

### `[P1 · M · Opus5-High]` The test suite has a render cache that 29 of 30 files never use *(filed 2026-09-01)*
`rendered()` at `scripts/harness/test_score_outputs.py:51` renders once and reuses — and **only `test_score_outputs.py` calls it.** Every one of the 15 slowest tests (~773s total) shells out raw via `subprocess.run`.

Worse, its key is the **whole-script SHA**, so every product edit busts it — i.e. every development session runs cold. On disk: **400 MB across 46 SHA directories**, never pruned.

**Concrete next actions, in order:** (1) count how many (asset, flags) pairs actually repeat across test files — the saving is unmeasured and could be small; (2) extend `rendered()` to the files that would benefit; (3) consider keying it on the reachable-code fingerprint the way `analysis_cache` does; (4) prune the 400 MB.

⚠️ **An earlier filing proposed content-hash keying the ANALYSIS cache to speed the suite. That was wrong** — the slow tests never import `analysis_cache` at all. The cache that matters here is `rendered()`.

### `[P1 · L (first slice: S) · Opus5-High]` The render gate spends 81% of itself on three PURE functions, and re-renders `main` from scratch every run *(filed 2026-09-01, profiled during the v6.3.0 merge)*
⚠️ **This item was first filed blaming the analysis cache, and the profile falsified that.** Recording the correction because the wrong premise is the interesting part: the analysis share is real but it is not the biggest slice, and `--verify` — which the gate does not even consume — is.

⚠️ **AND THE TABLE BELOW WAS THEN FALSIFIED TOO, on its own population.** It is a single asset, and the gate's population has at least three regimes: `marketing-automation.gif` (171f) runs `--auto` in **22.8s** against grenade's 45.2s *because it REFUSES at pass 1* and pays analysis only; `Pixel Saber.gif` (213f) takes 6.1s for the same reason. Frame count is not the driver — whether the asset renders at all is. Kept as measured, labelled as ONE POINT, because the corrected conclusion below depends on knowing that.

**Measured 2026-09-01 on `grenade.gif` (62 frames), timing the exact command the gate runs (`--auto`, 44.5s) — ONE ASSET, not the population:**

| slice | cost | share | pure function of |
|---|---|---|---|
| `--verify` (`--auto` pass 3) | 14.3s | **32%** | (input bytes, output bytes, verify closure) |
| analysis (`--recommend`) | 12.0s | **27%** | (input bytes, analyze closure) — already cached IN THE HARNESS |
| erosion calibration | 9.9s | **22%** | (frames, bg colour, calibration closure) |
| the render being measured | 5.4s | 12% | irreducible |
| startup / IO | ~3s | ~7% | — |

Isolated by pinning `--edge-cleanup-erosion 1` to remove the calibration, and by timing `--recommend`, a plain render and `--verify` separately. **The thing the gate exists to compare is 12% of what the gate costs.**

**What the population actually says, and it moves the ranking back toward analysis:**
- **62 records = 31 assets x 2 passes, and the resize pass re-runs the IDENTICAL analysis.** `analyze()` is a pure function of the source and `--resize-max-dim` is applied after it, so **~50% of all analysis work in the gate is a literal duplicate inside a single run.** No cross-run cache is needed to remove it — one in-process memo, or one CLI invocation doing both passes (the multi-file `--out-dir` mode already exists), takes it to zero at no staleness risk whatever. This is the cheapest real win on the list and it was invisible from the single-asset profile.
- **10 of 62 records (16%) render nothing at all** and pay analysis only: 4 are the GIF-entirely-transparent-frame refusal (`growth.gif`, `paper-plane.gif`), the rest are `--auto` coin-flip / changing-background refusals. ⚠️ Two DIFFERENT causes — the first read of this attributed all ten to one after checking a single asset. The gate does compare `returncode`/`no_output`, so a refusal flipping IS caught; it is not a vacuous pass. But for these records an analysis cache is a 100% win.
- **Analysis is the only cost every record pays.** `--verify` and the erosion calibration are paid only by the 52 that render.

**Falsified and NOT worth building — measured, not reasoned:**
- *Amortising Python startup across assets (a persistent worker pool, or batching via the multi-file CLI purely for startup).* Interpreter start plus `numpy`+`scipy.ndimage`+`PIL` imports measure **0.24s**, so 62 records pay ~15s of 626s = **2.4%**. Not a lever.
- *Capping BLAS threads.* numpy here is Apple **Accelerate**, which multithreads, and nothing sets a thread cap while 6 worker processes run on 6 performance cores. Real but modest: measured interleaved A/B/A/B at 3 workers, **67.1s -> 59.2s, 1.13x**, reproducible across both reps. Worth taking, not worth calling a fix.

**Tier 1 — free, no new machinery, no correctness risk. Do these first:**
1. **Commit a PRE baseline keyed on `main`'s script SHA.** The gate re-renders `main` from scratch on every invocation — a flat **50%** of total wall time recomputing something unchanged since the last merge. The artefact is a few KB of hashes. Measured this merge: PRE 626s + POST 296s.
2. **Longest-processing-time-first scheduling.** A run ends when its LAST unit ends, and the ordering is arbitrary today — the log shows the longest asset finishing last at 626s. LPT is a 4/3-approximation, free, and per-asset durations are already observable.
3. **Gate the resize pass on a fingerprint rather than on a flag nobody remembers.** `--no-resize-pass` already halves the work and is only needed when a change can reach the pixel-art/resize closure.

**Tier 2 — the real win, and it is ONE mechanism, not three.** An opt-in memo layer inside the PRODUCT (`--cache-dir`, off by default so the shipped `.skill` carries no cache behaviour into an ephemeral sandbox), keyed per-function by a closure fingerprint, covering `analyze()`, the erosion calibration and `verify()`. `scripts/harness/analysis_cache.py` already has most of the machinery. On a typical merge — where the render closure changes but those three do not — that is up to **81% off the POST run as well**. ⚠️ **An earlier version of this entry claimed "60-90s"; that was extrapolated from the falsified single-asset profile and is withdrawn.** A defensible estimate from what is actually measured is Tier 1 (-50%) plus the duplicate-analysis removal plus the thread cap, landing a 922s gate somewhere near **200-260s** with no new staleness risk at all; a cross-run cache buys more on repeat runs only. ⚠️ **Measure back to back before quoting any of it** — this repo has already recorded the same render set at 976s and 488s on consecutive runs, and this merge saw 626s and 296s for identical work at different contention.

**Tier 3 — considered and REJECTED. Recorded so nobody re-derives them:**
- *A whole-render content cache keyed on a render fingerprint.* Checked against a real change before believing it: the v6.3.0 branch edited `process()`, the render root, so the key would have moved and the cache would have bought **zero**. The render closure is most of the file. Low payoff, and its failure mode is the gate silently lying about the only thing it exists to detect.
- *Swapping Pillow for `avifenc`/`cwebp`.* Changing the encoder changes the output bytes, which is the quantity being compared.
- *Truncating assets to fewer frames.* This repo's own grenade defect lived only on frames 46-51.

⚠️ **The risk that governs all of Tier 2: a cache that lies turns a gate into a rubber stamp, and it fails SILENTLY — strictly worse than a slow gate.** Three mitigations, all already used elsewhere here: every uncertain case falls back to a whole-file SHA; a fingerprint that fell back is marked, so a run can report that it did; and a **RELEASE** (as opposed to a merge) runs `--no-cache` end to end. That keeps the fast path for the common case without ever letting a published artefact rest on it.

### `[P1 · M · Opus5-High]` The render gate has been recording ART LOSS on two assets and nobody has ever read it *(filed 2026-09-01, found by reading the gate's own baseline instead of only its verdict)*
`render_baseline.py` captures a `signal_lines` field per record and compares it PRE/POST, so a CHANGE in these lines would be caught. What nothing surfaces is the standing content. Two records in the `alphas` population carry this, today, on `main`:

| asset | source opaque px (the artwork) | survived | lost |
|---|---|---|---|
| `alphas/cinnamonexcited.gif` | 44,565 | 32,169 | **27.8%** |
| `alphas/PixelSaber-ezgif.com-gif-maker (2).gif` | 1,643,209 | 1,444,385 | **12.1%** |

`--verify` states it plainly — *"The SOURCE already had transparency, so its N opaque pixels were the artwork ... Colour-based removal has eaten real art"* — and the gate faithfully writes it into the baseline, where it has sat unread. A gate that records a defect and reports "0 changed" is working exactly as designed and is still telling nobody.

⚠️ **This is NOT the already-filed "two assets where the keyer removes solid artwork" item above.** That one is two DARK-corpus GIFs on flat coloured backgrounds (`ff6666`, `921219`) whose loss was diagnosed and closed. These two are **already-transparent sources**, i.e. the `--source-alpha-band` / `--ignore-source-alpha` machinery from §28.14 — a different mechanism entirely. Checked before claiming it, because the titles are close enough to merge them by mistake.

**Concrete next action:** two parts, and the second matters more than the first. (a) Diagnose the two assets — are they a real regression in the source-alpha scoping, or assets whose own alpha is wrong and which legitimately need `--ignore-source-alpha`? (b) **Make the gate report standing signals, not just changed ones.** A one-line summary of ERROR / ART LOSS / refusal counts printed after `--compare` would have surfaced this the first time it appeared instead of on the day someone happened to read the JSON.

### `[P3 · S · Opus5-High]` A module-level constant the closure cannot read still cold-invalidates the whole analysis cache *(filed 2026-09-01, hit while merging v6.3.0)*
`analysis_fingerprint` hashes **every** module-level statement unconditionally — its own docstring says a constant is "reachable from anywhere and cheap to include, so it is never analysed for reach." Measured 2026-09-01: adding `DEAD_PROTECTION_OPACITY = 0.05`, read only by `unprotected_design_regions` (a `verify()` helper `analyze()` cannot reach), moved the fingerprint `a99c2880ae5d6975` -> `a27d82c48415731b` and discarded all **29 entries / 42 MB** of warm cache. The closure itself was proven identical: 34 functions, zero changed.

This is the design's deliberate safe direction and must stay that way — a MISSED invalidation serves a pre-change answer to the gate meant to catch the change, which is far worse. But the docstring's word for the other direction is "cheap", and that was assumed rather than measured; here it cost a full cold pass on a merge that changed nothing `analyze()` can see.

**Concrete next action:** consider hashing ambient statements that are actually REFERENCED from inside the closure, plus all imports and class definitions unconditionally, with any doubt (a star-import, a name resolved dynamically, an unparseable node) falling back to hashing all ambient statements. ⚠️ **Re-run the 28-commit historical measurement before believing any improvement** — that is the evidence base the current key was chosen on, and the claim to beat is 9 of 28 (32.1%) staying warm. A refinement that cannot show a gain on that same population is not worth the extra failure mode.

### `[P1 · XS · Sonnet5-Med]` No `build_parser()` factory, so the parser cannot be introspected from outside *(filed 2026-09-04, asked for by the Devoid app)*

`ArgumentParser` is constructed inline inside the CLI entry function, so there is no zero-argument way to obtain it. Any external consumer that wants each option's type, choices, default and help text has to parse `--help` output, which is fragile, or hand-transcribe 63 flags, which drifts the moment one is added or renamed.

**The change is additive and small:** extract the construction into `def build_parser() -> argparse.ArgumentParser:` and have the entry function call it. No behaviour change, no flag change, and `p.parse_args()` still runs exactly where it does now.

**Who wants it:** `/Applications/Claude Code/Devoid` generates its option METADATA from argparse so a hand-authored control can never drift from the flag it drives, and a newly added flag surfaces as a warning that no panel covers it. ⚠️ It deliberately does NOT generate a control per flag — that would produce a 63-control passthrough form, which is the thing that app exists to avoid. See `docs/PLAN.md` stage 1.6 there.

⚠️ **This was previously described in Devoid's `HANDOFF.md` as already filed here. It was not** — that claim pointed at the `--auto`/`--verify` entry below, which is unrelated. Filed properly now.

### `[P3 · XS · Sonnet5-Low]` The hand-labelled corpus is cited as 714 judgements; the files hold 981 *(filed 2026-09-04)*

`scripts/harness/populations.py:25` calls it "714 judgements, the most expensive artefact this project owns", and the figure appears in four places across the repo. Counted from `scripts/harness/labels/*.json` on 2026-09-04: **1,038 entries, of which 981 carry an `edge_hardness` classification** (`pixel_art` 552, `antialiased` 391, `unsuitable_no_edges` 31, `ambiguous` 7) and 57 are prose notes rather than classifications.

Stale by 267 — 38%. Every recall and specificity figure this project quotes has that corpus as its denominator, so the number matters more than its size suggests. **Derive it rather than restating it**, per this repo's own rule about counted claims that rot.

### `[P2 · S · Opus5-Med]` `--auto` runs a full `verify()` and prints it as PROSE, so a caller has to run the identical pass again *(filed 2026-09-03, found while building the `dior gif` CLI wrapper)*

`--auto`'s pass 3 calls `verify(input, output, tolerance=..., assume_remove_colors=...)` and prints five of its fields to stderr as English (`scripts/remove_gif_background.py:10026-10048`). Those are the RIGHT numbers — computed with the run's own `--assume-remove` context, which a caller re-deriving them can easily get wrong. But they are unaddressable: a wrapper that wants a verdict it can branch on has to run `--verify` again, which is a second full per-frame pass over the same pair.

**Measured on a 144-frame 640x640 asset: `--auto` ~60s, then `--verify` ~30s on top — a 50% overhead on the whole job, for a computation that had already been done seconds earlier.** On a five-file batch that is two and a half minutes of pure duplication.

**The fix is small and additive:** `--auto --verify-json <path>` (or a `verify` key in a machine-readable summary) writing pass 3's existing `_v` dict, which is already in hand and already correct. No new computation, no behaviour change to any existing flag, and nothing to decide — the object exists and is discarded.

⚠️ **Why the wrapper does not simply parse the prose.** The printed lines are lossy in exactly the place that matters: `full verify -- worst protected-region coverage: 0.0` gives the number but not which region it belongs to, and `unprotected_design_regions` — the pre-filtered list that says which parts actually came out wrong — is never printed at all. Parsing prose to make a correctness claim is the kind of thing this repo has already recorded going wrong (a check that cannot fail is worse than no check), so the wrapper pays the 30s instead. That is the right call for the wrapper and the wrong steady state for the tool.

**Consumer:** `~/.config/dior/scripts/gif_wizard.py` (`verify()` / `report_verify()`), in the dior-cli repo. It is not part of the skill package and the skill does not know it exists — this item is about `--auto` being unable to hand its own answer to ANY caller, not about that wrapper specifically.

### `[P2 · S · Sonnet5-High]` `--recommend` says nothing about size, on a tool whose second half is size fitting *(filed 2026-09-01)*
`--recommend` returns a format ranking and a flag command and never mentions `--target-kb`, `--min-width` or `--min-quality`, even though a stated byte cap is one of the three cases SKILL.md's own size gate enumerates. A real session with "192px wide, under 256 KB, q65-85" in the brief hand-rolled a quality loop, timed out a tool call, and shipped two of four assets at 128 px wide when one fit call reached **192x215 at q85 / 236.2 KB** and full resolution at **498x558 / 196.9 KB**. Cheap candidate: when `--recommend` is given `--target-kb`, have it emit the fit flags alongside the render flags; failing that, have it name the fit call in evidence the way it names the format ranking.

### `[P1 · M · Opus5-Med]` Task 14c — derive the unprotect region from the ANIMATION, not one frame *(filed 2026-08-23, split out when the rest of its parent P0 shipped)*

📄 `docs/plans/2026-08-22-target-kb-constraints-and-format-ranking.md` Task 14c · `references/lessons.md` §43 · 📁 `local/2026-08-22-fade-edge-cases/`

`--recommend`'s `--unprotect-region` hint derives its box from `bbox_xyxy`, the region's extent on the SAMPLED frame. Rendered with it, an interior that breathes across the animation (18,038 → 20,367 px on broadcast) leaves **2,340 px of residue**.

⛔ **A PADDING CONSTANT WAS BUILT AND REJECTED ON MEASUREMENT. Do not rebuild one without repeating this table.**

| box | residue (worst frame) | artwork (navy) |
|---|---|---|
| bbox as-is | 2,340 px | 23,399 |
| true across-frames extent | 1,607 px | 23,427 |
| +8px uniform | 1,905 px | 23,416 |
| +15% pad | **0** | **22,191** — 1,287 px of artwork gone |
| hand-measured | **0** | **23,478** |

**So the region must be a MASK, not a box.** The exact across-frames extent still leaves 1,607 px, because the residue is scattered small blobs a rectangle does not describe, and the +15% pad only clears it by reaching the artwork's outer silhouette. **Do:** record, per candidate region, the union across frames of its background-coloured interior; emit a `;`-joined multi-rect covering that mask's connected components (`parse_protect_regions` already splits on `;`, so the flag needs no change). **Falsifier, both halves:** rendering with the tool's OWN suggested region leaves under 200 px of residue AND holds artwork within 1% of the hand-measured render. Then `run_populations.py` for how many assets gain a hint and how much larger the regions get, and **show Harkirat the render before merging** — the two defects in Task 14 itself were both found by him looking at one.

### `[P1 · M · Sonnet5-High]` Run the three-tier agent trial against the SHIPPED package — now TWO releases behind *(filed 2026-08-21, deferred by Harkirat at release time; scope widened 2026-08-23)*

✅ **RUN 2026-08-23 against `local/gif-background-remover-v6.1.1.skill` — findings and fixes are on `fix/gate8-trial-findings` (commit `e58e4ed`), not yet merged.** Three fresh isolated sessions (vague/detailed/expert tiers) processed the full 10-asset corpus. Self-reported STEPLOGs looked clean (the refusal mechanism itself worked — all four aiming points below were exercised and recovered correctly), but Harkirat's manual review of every rendered output found real, systematic defects the STEPLOGs, `--verify`, and my own first-pass automated checks all missed — this is itself evidence for why gate 8 exists. Five confirmed, fixed and verified (real before/after renders + a clean 31-asset render-diff): (1) a structurally-weak outline enclosure silently drops same-background-colour art on growth/paper-plane/rocket, fixed with a union-across-all-frames fallback in `build_protected_masks_robust`; (2) `--protect-outline-color`/`--protect-region` mutual exclusivity lifted (prerequisite for #1); (3) `--recommend`'s `suggested_command` can silently produce a GIF while `recommended_format` still says webp/avif (satellite.gif, likely broadcast.gif) — now gets an explicit evidence note; (4) explicit `--output` paths had no overwrite guard, now warn; (5) SKILL.md documents the previously-undocumented "nameable fade" refusal and the `--recommend`/`--analyze` redundancy. **Two items still open, need Harkirat's direct input, not guessed at:** erosion-calibration/haloing on flat vector art (galaxy/secure/broadcast/notification — the fringe metric reads 0.0 at every level, my own zoomed comparison didn't show an obvious difference either, so this may be a real metric blind spot or a style preference, not confirmed either way), and broadcast.gif's remaining defects (its regions are fully verified/enclosed, so this is NOT the same mechanism as #1, root cause not yet found).

✅ **Hurricane's fade classification RESOLVED and CORPUS-VALIDATED 2026-08-23, commit `e9f8520`.** The trial's own conclusion ("painted, correctly left opaque") was wrong — Harkirat supplied the fact that broke it: hurricane has THREE independently-fading regions (`#DA4B85` left half, `#FD6252` right half, `#042A75` outline), not one, and the tool's ladder-detector was reading each one's own paler stages as a "hand-picked tint chain" and freezing them solid. Fix: `recover_fade_alpha_frames` now absorbs any palette candidate near-collinear (cosine ≥0.97) with a named fading colour, and paler than it, into the same family — propagated into both `protect_parents` and the final `fading` set (a first version only did the former, itself a bug caught in the same pass). Visually verified clean across hurricane's hardest frames (30-51, including its documented worst tumble-risk frame 39) with zero pinwheel-icon damage across all 120 frames. **Corpus-validated against the exact four real `--recover-fade-alpha` assets this code area has a documented regression history on** (`references/lessons.md` §34.2's own table — crystal/gift/love/heart): forced `--fade-color` on each with the pre-fix and post-fix script and diffed every frame's alpha plane exactly. Crystal/love/heart: zero palette entries met the absorption threshold, byte-identical output. Gift: one entry (`fa7fa0`) DID get absorbed (cosine 0.98 with the named `fd6050`) — a real behaviour change in the log — but the rendered alpha came back byte-identical across all 172 frames regardless, because `fa7fa0` never appears blended with the background anywhere in the animation (always a pure, fully-saturated pixel, so unmix assigns it t≈1.0 either way). 0 of 461 combined frames differ. Not yet done: extending the same absorption logic to `detect_fading_colors`'s auto-detect path (currently only the manual `--fade-color` branch has it), and the full 106/797-asset gates.

✅ **Two further defects found and fixed the same day (2026-08-23), both confirmed directly by Harkirat looking at the actual renders — not automated checks.** Harkirat rejected the first "frame 53 fixed" delivery outright ("i do not want the frame 53 compromise") and asked for the whole class solved before stopping.

1. **A wall made of the badge's OWN colours could seal off part of the badge's OWN fading interior.** `recover_fade_alpha_frames`'s flood-reachability barrier is topological (an enclosed pocket defaults to protected/opaque, needed to protect real interior design like crystal.gif's white interior) — but it had no notion that a barrier pixel can itself be part of the SAME fading family as what it encloses. Two distinct causes, found by tracing the exact pixels, not by pattern-matching to a prior theory: (a) a thin ring of pixels where two art colours meet (e.g. the outline touching the fill) reads as "unexplained" because `unmix_against_palette` only tests ONE colour blended with the BACKGROUND, never an art-to-art blend — new `fading_seam_mask()` makes such a seam permeable when both sides are named fading colours, a no-op whenever fewer than two colours are named (every corpus asset this code has a regression history on). (b) a pocket sealed only by non-solid barrier pixels (never `solid & t>=BARRIER_ALPHA`) got re-tested for reachability with only genuinely SOLID barriers blocking; if reachable that way AND less than 50% of the pocket is background-degenerate (`t<0.05`, the signature of protected same-colour-as-background design like crystal's interior, measured 97%+ there vs <8% on hurricane's real fading pockets) AND the pocket is at least 50px (excludes dozens of 1px AA specks on crystal's sparkle edge that individually passed the background-fraction test trivially and left a thin mis-coloured rim — caught by a diff, not assumed away), it gets exposed. **Corpus-validated the same way as the fix above**: crystal/gift/love/heart all byte-identical (0/461 frames differ) at every stage of this fix, including the two false starts along the way — one gated on pixel-position frequency (`art_prior`) and broke crystal for real (13,913px in one frame) because crystal's own outline shifts position slightly frame to frame too, the same problem as hurricane's spinning icon; reverted immediately, not left in the branch.
2. **The family-absorption cosine floor (0.97) was too strict for a bevel highlight on the SAME named outline.** Harkirat caught this by eye after (1) shipped ("some frames show the navy outline flashing white or part of it flashing/turning white while the other part continues to fade") — traced to hurricane's outer octagon border having its own bevel-highlight shade at cosine 0.9607 with the named `042a75`, just under the 0.97 floor, so it stayed classified solid and rendered fully opaque while the rest of the border faded normally. Widened to 0.95 (still comfortably above the 0.762–0.93 ceiling measured for every genuinely-distinct real colour on this asset). Re-corpus-validated: crystal/gift/love/heart still byte-identical at the new threshold too, since this loop isn't gated by colour count the way `fading_seam_mask` is.

✅ **Item 3 (border banding) investigated 2026-08-24 — partially addressed, real fix shipped for a DIFFERENT and more severe defect the investigation actually found.** Started from the item-3 description above (octagon border banding via `unmix_against_palette`'s single-nearest-colour match). Added `unmix_top2_against_palette` plus a softmax(-residual/T) blend between a pixel's best and 2nd-best match when both are named fading colours (`FADE_BLEND_SOFTMAX_T`) — a real, corpus-safe, always-on (no hard gate) smoothing of the boundary between two family members, kept in the final commit. Measured effect on the border specifically: mixed, not a clean win (2 of 4 sampled frames improved, 2 slightly worse) — a boundary blend can only ever soften the SEAM between two flat regions, it cannot smooth a whole arc that the source GIF painted as one flat shade, so item 3 as originally described is still open in the strict sense.

**What the investigation actually found, from Harkirat's own annotated screenshot, was a different and more severe defect: hurricane's inner pinwheel/vortex icon reuses the SAME `#042a75` colour family as the outline, and was rendering with visible mixed opacity across its own blades — it should never fade at all, always fully opaque, exactly like the centre hub already correctly renders.** Five broken attempts before the real fix, each a genuine distinct root cause, kept here because the mechanism matters more than the failure: (1) grouping same-family `exact` pixels into one connected component and setting ONE alpha via a low percentile — first version had no cross-family restriction and merged the pink fill, orange fill and navy outline into one 273,222px blob (nearly half the canvas), whose own true silhouette edge dragged everything to alpha 0; (2) restricting components to one NAMED anchor family via nearest-EUCLIDEAN-distance — wrong test, because a very pale colour sits numerically close to any anchor regardless of hue, so real pale-navy members (`#9078a7`, `#ceb7c3`) got assigned to the pink family anyway, reproducing the same giant-blob collapse; (3) fixed the family test to COSINE similarity against each anchor's own ray from background (matching how the absorption loop itself defines a family) — this correctly isolated the pinwheel (34,346px) as its own component, confirmed by adding real instrumentation (`fade_debug`) to the function rather than trusting an external reconstruction that had stopped matching the real output three times in a row; (4) even correctly isolated, a percentile-of-one-component approach fundamentally targets the wrong goal ("uniformly faded") when Harkirat's actual requirement was "never faded at all", so the mechanism was abandoned entirely rather than patched further. **The actual fix: two new general-purpose flags, `--fade-protect-region` (circle/rect spec, reusing `parse_protect_regions`) and `--fade-protect-colors` (restricts the region to one named colour family, since the region alone force-opaqued the fill too and painted a visible white/pale disc the first time).** Verified across 8 sampled frames including a structurally anomalous tumble frame (40, where the border itself fragments into multiple disconnected pieces): vortex alpha is 255 on every art pixel, every frame, while background correctly shows through the pinwheel's own concave gaps (alpha 0, ~13,700px every frame) and the border keeps fading normally in the same region. The unused percentile/component-uniformity code (items 1-2-3 of the false-start list above) was removed rather than left in as unproven complexity — the real fix needed none of it. Corpus-validated at every single step of this whole investigation (crystal/gift/love/heart, forced `--fade-color`, exact alpha diff): 0/461 combined frames differ, every time, including every broken intermediate version. Two things checked and confirmed pre-existing, NOT introduced by this session (both present identically in the pre-session baseline): a small yellow sparkle near the border at frames 59-60 (Harkirat confirmed this is real intentional art, not a bug) and the border legitimately fading all the way to zero opacity for several consecutive frames (42-45) as a smooth, continuous, non-anomalous part of its own fade cycle — Harkirat's call was not to spend further budget chasing this specific edge case tonight. **Next session, if picking this back up: the strict item-3 banding (a soft/weighted multi-colour blend or proper barycentric unmix) is still genuinely open and still the same scope/risk as originally described — the softmax blend added this session is a real but small improvement to it, not a fix.**

✅ **`/verify` (2026-08-24) actually drove the two new flags with real, adjacent-to-happy-path CLI inputs — not just the corpus/hurricane content check above — and found three real gaps, all fixed and re-verified.** `--fade-protect-region banana:1,2,3` (a typo'd kind) crashed with a raw, uncaught Python traceback instead of the clean `SystemExit` every other misuse case in this tool gets — now `parse_protect_regions` errors are caught and re-raised with the flag name and expected syntax. `--fade-protect-colors 123456` (a hex matching nothing in the palette) silently protected the nearest family regardless of distance, with zero feedback — unlike `--fade-color`, which already refuses past a 30-unit threshold; `--fade-protect-colors` now applies the identical check and refuses the same way. `--fade-protect-region` passed without `--recover-fade-alpha` silently did nothing — now warns, mirroring the existing `FADE_EXCLUSIVE_FLAGS` warning pattern this file already uses in the other direction. All three retested clean, then the full corpus/harness/hurricane-happy-path check re-run end to end: 0/461, vortex alpha still 255/0 as designed, harness suite unchanged (same 2 pre-existing failures).

**Real mistake this session, contained not compounded:** an earlier same-session A/B comparison (swapping in the `e58e4ed` script to render a "pre" baseline for a broader corpus diff) left the live `scripts/remove_gif_background.py` on disk reverted to that older commit for several minutes while its own restore step was still queued behind a slow 31-asset render. Caught by noticing `fading_seam_mask` had vanished from a `rg` search that should have found it; the real fix was still safe in a `/tmp` backup taken before the swap. Restored immediately, the stray process killed so its own eventual restore-to-backup couldn't later clobber further edits. No data was lost, but it is exactly the class of accidental-overwrite risk `feedback_never_overwrite_without_permission.md` exists to prevent — logged here because the near-miss was in swapping the *live product file* itself, not a `local/` scratch folder.

**Still not done, as of 2026-08-21/23:** full 106-asset render set and 797-asset corpus score (only the fast/targeted checks ran that session), auto-detect-path family-absorption, item 6 (erosion detector), item 8 (broadcast's remaining defects). ⚠️ **STALE as of 2026-08-24/25 — three of those four are now DONE**, on this same branch, in later sessions: the full 106-asset render diff and 797-asset corpus score both ran clean (§45's validation ledger); auto-detect-path family-absorption shipped as §45.1; the erosion detector shipped as §45.2. Only item 8 (broadcast's remaining defects) is still genuinely open. **Also surfaced en route, unrelated to the trial itself:** an accidental `rsync` overwrote the original 2026-08-19 trial's `STEPLOG.md` files in `local/Corpus Trial Gifs/agent-*/` and, discovered later, one harness test fixture (`agent-3-expert/growth_transparent.webp`) that two falsifier tests depend on — those 2 tests now fail against the replaced fixture, unrelated to any code change; no backup exists.

⚠️ **A THIRD TRIAL IS STILL DUE, and v6.2.0 shipped WITHOUT it (Harkirat's explicit call, 2026-08-25, gap named before the decision).** This branch shipped real behaviour changes since the 2nd trial (2026-08-23) that no trial has ever exercised: the hurricane fade-family fixes above, §45's auto-detect absorption / erosion blend-fraction / border-band generalisation, §46's `--analyze` redundancy reminder, §47's repeated-region-flag refusal, `recommend()`'s new region-backstop suggestion, and 6 more real bugs an 8-angle code review plus two rounds of harsh self-audit found the same night — all real evidence the release is careful, none of it the KIND of evidence gate 8 provides. **A 3rd trial's most useful single questions, in order of how much of tonight's work they'd actually validate:** (1) does the `--analyze` redundancy reminder actually change what a fresh session does on assets 2 through N of a job, or does it get read and ignored the same way the SKILL.md paragraph it replaces was; (2) does a fresh session that types a region flag twice (a very ordinary CLI habit) actually read and correctly act on §47's new refusal message, or does it get stuck; (3) does `recommend()`'s new backstop suggestion ever actually fire on a real asset a trial session hands it, and if so, does the session correctly paste both flags. Those are real, falsifiable behavioural questions this repo cannot answer by reading its own code — only a fresh trial run can. Run against the v6.2.0 package specifically; do not assume the v6.1.1 package used by the 2nd trial is still the right target given the amount of unshipped work already on this branch.

✅ **All three items above CLOSED 2026-08-24 (continuation session) — auto-detect-path family-absorption, item 6 (erosion detector), and a further pass on item 3's strict border banding.** Full detail and formulas: `references/lessons.md` §45.

**Auto-detect-path family-absorption (§45.1).** The collinear-family absorption loop that fixed hurricane's ghost boundaries (this page, above) only ran behind an explicit `--fade-color`; auto-detected fades (`detect_fading_colors`, no named hex) got none of it. Extended the identical cosine-≥0.95 absorption to the `else:` branch of `recover_fade_alpha_frames`, anchored on each auto-detected colour's own pre-absorption ray instead of a named hex, and generalised `family_of`'s anchor list (`want` vs the new `_auto_anchors`) so both branches share one cosine-nearest-anchor computation. Corpus-validated on crystal/gift/love (auto-detect, no `--fade-color`): crystal and love byte-identical (0 diff — no absorbable candidate for their single anchor); gift genuinely absorbs `fa7fa0` into the `fd6050` family now (previously only the named-hex path did), changing 36,990 alpha px across 77/172 frames, all 1-3 units — a small, boundary-only refinement, not a dramatic reclassification, matching the SAME real absorption the named case already validated as safe. Harness suite: same 2 pre-existing failures, 0 new.

**Erosion-calibration detector (§45.2), the P1 that had been open since the original gate-8 trial.** `calibrate_edge_cleanup_erosion` picked its erosion level from ONE signal, `measure_outer_ring_background_fraction` ("is this ring pixel closer to background than to art") — blind by construction to a ring pixel that is MORE than half art but still an unresolved antialiasing mix, which is exactly galaxy.gif's case: that signal reads **0.0000 at every erosion candidate**, so calibration never had anything to act on and galaxy stayed at erosion 0. Added `measure_outer_ring_blend_fraction` (does this ring pixel unmix as a genuine partial blend at all — `res` within tolerance, `t` strictly between 0.08 and 0.92 — reusing `detect_fading_colors`'s own blend vocabulary), and combine the two per-frame by MAX before averaging. Measured against the real `--auto --edge-cleanup-erosion N` output at N=0,1,2 (not a reconstruction): galaxy's new signal reads **0.5989 → 0.1359 → 0.0003**, a clean monotone separation the old signal could not see at all. End-to-end confirmed via the real CLI's own `erosion calibrated against this asset's own curve` log line: **galaxy now correctly selects erosion=1 (was 0)**; the two healthy controls (rocket, secure) land on the identical erosion=1 they already got — unchanged, not coincidentally matching, confirmed via the same real log line before/after. `broadcast.gif` also unaffected (its real defect is the separate enclosed-region issue this doesn't touch, not erosion) and `notification.gif`/`megaphone.gif` could not be probed directly — both now hit `--auto`'s v6.1.0 refusal gates (nameable-fade / partial-enclosure) before reaching erosion at all, an orthogonal, pre-existing gap, not something this change caused or fixes.

✅ **Item 3, border banding — CLOSED 2026-08-24 (§45.3).** The already-shipped top-2 softmax blend was measured, on real ADJACENT hurricane pixels, to still step 22→122: each pixel's own blend was individually correct, but WHICH TWO of the border's 9 absorbed family members ranked "top-2" flipped between neighbours once a family has that many close members, so limiting the blend to the winning pair just relocated the cliff to the pair's own boundary. Generalised `unmix_top2_against_palette` into `unmix_family_blend` — softmax over the WHOLE fading family, not just the best two, a strict generalisation (reduces to the old behaviour with ≤2 close members) — and replaced the top-2 call site with it; `unmix_top2_against_palette` is now dead and was deleted rather than left unused. Measured effect on the SAME traced pixel pair: 22→97 (was 22→122), a real ~30% reduction, and a broader scan of hurricane frame 35's mid-alpha region found total large (>60-unit) neighbour jumps fell from 2,460 to 2,149 (~13%). **Confirmed CLOSED, not just measured — visual inspection settled it.** The pixels that still jump were traced (best single-ray residual 6x worse than a clean neighbour's, e.g. 6.61 vs 1.08), consistent with real antialiasing between two DIFFERENT design elements (the pink fill touching the navy border) rather than within-family ambiguity a wider blend can smooth. Rather than assume that from pixel statistics alone, the real render was composited over a checkerboard and inspected at 4 frames spanning the fade cycle (0/35/39/78) plus zoomed crops of the specific traced boundary: no jarring banding anywhere, the border reads as a smooth uniform ring at every fade depth, and the traced boundary is a real antialiased edge between two elements, not a stepped artefact. The k-nearest/barycentric redesign `unmix_against_palette`'s docstring called for turned out not to be needed. Corpus-validated: crystal/love byte-identical alpha (≤1-unit RGB rounding noise from the reordered math); gift genuinely changes (max Δalpha 32, mean 2.65 over changed px, no wild swings, no 0↔255 flips) — a real, bounded refinement of the SAME already-validated family, not a new regression surface. Full 106-asset render-diff and harness suite re-run against the final combined state; see the ledger in §45 for the numbers.

⚠️ **UPDATED 2026-08-23: v6.1.0 will make this the SECOND consecutive release shipped without gate 8, and v6.1.0 is the riskier of the two on exactly the axis this gate measures.** It changes what `--auto` DOES in the most user-visible way possible — `--auto` now REFUSES on a coin-flip enclosure or a nameable fade (measured 10.2% of 304 assets) and expects `--assume-protect` / `--assume-remove` / `--assume-no-fade` back. Whether a fresh session reads that refusal and answers it, or reads it as a crash and gives up, is not something the corpus score, the render diff or a code review can see — it is precisely and only what a three-tier trial measures. **Add a fourth aiming point to the three below: hand one tier an asset that refuses and check it recovers.**

**The highest-yield gate on the release checklist, and v6.0.0 was published without it — deliberately, and this item is the record of that choice.** `CLAUDE.md`'s release gate 8 calls for three fresh sessions, each given ONLY the built `.skill` and the same real GIFs, under three tiers of user request (vague / detailed-prose / expert-with-named-hazards), each logging every step. The 2026-08-19 run of it found **five defects, three severe, in ninety minutes** on a build that had just passed the 797-asset corpus score, an xhigh code review with ten finder angles, and a full PRE/POST render diff — because all three of those regimes compare the product against itself or against a label, never against what a user wanted.

**Run it against `local/gif-background-remover-v6.0.0.skill` specifically**, since that is what is live. The three surfaces most worth aiming a tier at, because they are new in v6.0.0 and none of them existed when the last trial ran:
1. **The multi-file path.** Does a fresh session actually reach for `a.gif b.gif c.gif` in one call, or does SKILL.md's "One invocation per JOB, not per FILE" rule get read and then ignored? The rule is stated; whether it changes behaviour is exactly what a trial measures and nothing else does.
2. **The size/format gate.** Give one tier a deliberately ambiguous size hint ("make it work for chat") and check the session ASKS rather than inventing a target. That is the failure mode the gate was written for, and prose cannot prove it holds.
3. **`--target-kb`'s reordered ladder.** Give a tier a byte cap on flat vector art and check the delivered file is full-resolution rather than a quarter-scale one — and check the `NOTE: downscaling made this file LARGER` line is read rather than scrolled past.

⚠️ **Do not reuse the 2026-08-19 output scorer without falsifying it first.** It reported hurricane 100% correct on all three agents while human review called it a disaster on all three: a binary keep/remove ground truth cannot grade a FADE, it had no edge-quality measure at all, and it reported a MEAN over sampled frames so a defect on 16 consecutive frames read as 99.9%. Report the WORST frame, and check the scorer can fail on the defect you are testing for. Method, caveats and the full write-up: `docs/investigations/2026-08-19-three-agent-package-trial.md`; the product half is `references/lessons.md` §34.

### `[P3 · S · Opus5-High]` Two assets where the keyer removes solid artwork far from the background *(filed 2026-08-20, the residue of the erosion investigation)*

All that survives of the 20-asset "keyer damage" population after attribution (§37.5): two dark-corpus GIFs on genuinely flat backgrounds whose lost pixels are NOT a truncated fade. One (background `ff6666`, border flatness **1.000**) loses a **2,072px** solid component with 88.2% of its lost pixels past distance 150; the other (background `921219`, flatness **0.945**) loses **4,164px** at 74.9%. Both are `appears_hard_edged: False`, both get real recommendations — `--protect-outline-color f064f0 --erosion-exempt-max-size 3` and `--tumble-safe --erosion-exempt-transient` respectively — so this is not a "no flags emitted" case.

✅ **Diagnosed by hand 2026-08-20/21 — confirmed, the two do NOT share a mechanism; one per-asset number below is unverified, see the frame-alignment bug item above.** Each asset's own `--recommend`-suggested flags were rendered and diffed OUTPUT-vs-OUTPUT against the bare render at erosion 0 (no source involved, so immune to the frame-coalescing bug filed above): byte-identical alpha on both assets (0/14 frames differ on `_(25)`, 0/60 on `_(10)`), so the recommended flags fix neither loss. `_(25).gif`'s loss (colour ≈ its own outline colour f064f0) is at original frame **14**, not 12 as first reported — the raw-position read was itself off by the same coalescing bug — and the apparent tie to `--recommend`'s own "frame 12" evidence is **retracted**: both the frame number and the size (19px cited vs. ≈2,130px measured, a 100x mismatch) fail to line up once checked. `_(10).gif`'s loss (near-black, stored index 48, reproduced twice by independent methods) could NOT be re-verified against true source alignment — an attempted correction hit its own unresolved bug — so its exact frame/colour are an open question, though the "flags don't fix it" conclusion holds regardless. ⚠️ **Update 2026-08-21:** the general frame-coalescing bug behind this is now FIXED (see the resolved list) -- re-deriving `_(10).gif`'s own frame/colour through the corrected scorer was not done in the same pass and is a cheap follow-up, not a re-open of the mystery. **Recorded as known limitations, per the original instruction here** — not inventing a shared rule from a sample of two. Full diagnosis: `references/lessons.md` §37.7.

### `[P1 · L (first slice: M) · Opus5-High]` A 40-frame spread cannot see a transient, and the trigger for looking harder cannot be computed from the spread *(filed 2026-08-20, Harkirat's question)*

`analyze(max_samples=40)` samples the expensive per-frame work — the candidate-region enclosure search, edge hardness, the band-interior/fade detection. Several checks already scan every frame (tumble margin, the small-region histogram, the blank-frame scan added 2026-08-20), so this is about the sampled half only.

**The failure is a RATIO, not a class of asset: a transient of length L in an N-frame file is invisible when `40/N < 1/L`.** Measured 2026-08-20 — `for-you.gif` (144 frames) loses its navy outline for **37 consecutive frames** and a 40-sample spread lands ~10 samples inside it, so it was caught; `in-love.gif` (48 frames) has fade stages living 1–2 frames each and 40-of-48 samples nearly everything; but the changing-background asset filed the same morning has a 5-frame window in 30, and a 10-sample scan read **78.6% at frame 10 while frame 12 was 0.0%**. The same content at 300 frames misses all three.

⚠️ **So a "this asset looks hard" trigger computed FROM the 40 samples is unfalsifiable in the direction that matters** — the assets needing escalation most are exactly the ones whose difficulty is invisible at 40 samples. Do not build that.

**Do: make the CHEAP pass dense and let it nominate frames for the EXPENSIVE pass.** The blank-frame scan is the worked example — every frame, no measurable cost, because what makes `analyze` slow is the enclosure search, not decoding. One extra cheap pass over every frame collecting the detected background colour, the pixel count of each provisional palette colour, and the non-background pixel count yields four nominations, **three of which are defects already filed here and currently undetected**:
1. **background colour MOVES** beyond tolerance → the changing-colour-background item above.
2. **a colour that carried the outline COLLAPSES** on a run of frames → `for-you.gif`; today it survives only because `--recommend` happened to pick a colour present in all 144 frames.
3. **a colour appears in very FEW frames at LARGE pixel counts** → `in-love.gif`'s fade stages; this is the fade-ladder signature from `references/lessons.md` §34.2.
4. **non-background count hits ZERO** → the blank frame. Already shipped.

Each yields FRAME INDICES, so escalation adds a handful of frames to the spread instead of raising 40 → N.

✅ **FIRST SLICE DONE 2026-08-21 (Part 2).** `nominate_sample_frames` reads the dense scan's own summary and names frames; `sample_idxs` unions them in, capped at 8 and thinned across the span. **Cost measured, not assumed:** the dense pass is a median **3ms** across 910 assets, max **0.62s** on the longest (a 250-frame asset) — and it reuses the loop and mask `analyze` already ran, so nomination itself adds nothing.

⚠️ **THE MOTIVATING ASSET CANNOT EXERCISE IT, and that is a finding rather than a failure.** `Pew Pew Pew.gif` is 30 frames — under `max_samples=40` — so every frame is already sampled and there is no gap to close. Forced to a 10-sample spread it nominates frames 11, 12, 13 and 15 and **recovers 3 the spread had missed**, which is the mechanism working; at the shipped setting it is simply not a case. The real production case is `Pixel Saber.gif`: **213 frames, two 3-frame transients (58-60, 153-155)**, where 40/213 = 0.188 against 1/3 = 0.333 — exactly the ratio failure this item describes. It gains 5 frames. **Scoped by measurement rather than assumed:** the dense pass was run over ALL ten populations (910 assets) and exactly **1** gains frames at `max_samples=40`, so the affected population is one asset plus the eight the crash fix recovers.

⚠️ **TWO GUARDS, both added because a real asset walked straight through the naive rule.** (1) `recolored_frame_indexes` is populated from the raw per-frame comparison **even when the verdict is `null`** — measured on an asset reporting `changes: null` ("there is no background colour for the animation to change") with 44 of 60 frames listed as recoloured. Nominating off that escalates the expensive pass on a question the summary already called not applicable. (2) The same for an already-background-removed source, delivered through a cheap dense-loop hint because the authoritative flag reads `sample_idxs` and cannot be consulted before it exists. Without either guard, 3 assets nominated instead of 1.

✅ **AND IT UNCOVERED A PRE-EXISTING CRASH, unrelated to this work but found by it.** `summarise_background_color_stability` bails only when FEWER than half the frames have a readable background corner; anything in the 50-100% band reached `None > int` and raised a bare `TypeError` with no file name and no frame number. **3 real corpus assets crashed `--analyze` this way**, silently, and now analyze. ⚠️ This first read **8**, taken from the render harness's `no_output` list — a DIFFERENT failure mode (no output FILE from a render), conflated with an analyze crash. Re-running each against the pre-fix script showed only 3 were this bug; the other 5 analyzed fine before and after, so their `no_output` has another cause and is still open. **Two failure lists that overlap are not the same list.** Same class as §39. 12 falsifiers in `scripts/harness/test_frame_nomination.py`, including the exact 50-100%-readable shape and a check that the fix did not buy its silence by disabling the rule.

### `[P3 · S · Sonnet5-High]` `CatPackFree` sits at 0.250 (1/4) — four assets *(filed 2026-08-19)*

The last sprite pack below 100%. The seventh discriminator (§32.7) took `Tiny RPG Character Asset Pack` from 0.235 to 0.941 and did not move this one. The three misses are `Box3.png` (cc 49, cliff 0.176, band 0.359), `Idle.png` (cc 52, cliff 0.114, band 1.192) and `drculacat.png` (cc 20, cliff 0.281, band 0.898) — note the BAND is wide on all three, which is why the narrow-band rule abstains and why they are not the same shape as the pack that was just fixed. ✅ **The four were inspected by eye at 5x on 2026-08-19, and all four labels are CORRECT** — a cat-in-box strip, an idle strip, a Dracula-cat strip and an isometric furniture sheet, all unmistakably drawn on a pixel grid. So these are three real misses, not label noise, and §32's nine-mislabels warning did not repeat here.

**The mechanism, and why the seventh discriminator abstains.** All three misses are hard-alpha cutouts whose transition band is WIDE — 0.359, 0.898 and 1.192 against the rule's 0.20 ceiling — because the cats carry two or three tones of soft internal shading that reads as a broad band. §32.7's rule fires only on a NARROW band, so it correctly says nothing here. Raising the ceiling to 0.25 was measured: it buys 2 detections and costs independent specificity 0.9681 → 0.9645, and the flat-specificity plateau that justifies 0.20 ends there. **Do:** leave the threshold alone. This needs a measure that separates soft internal SHADING from an antialiasing ramp, which nothing in the current set does, and 3 assets is not enough population to build one against.

## ✅ Considered and NOT fixed — a real decision, not an oversight

*Findings that were surfaced, genuinely weighed, and deliberately left alone — listed here so they don't get re-flagged as forgotten work by a future review. These deliberately do NOT move to the archive: a decided-no that gets filed away is a decided-no somebody re-opens.*

- **SKILL.md's frontmatter `description` summarises what the skill DOES, which `superpowers:writing-skills` warned could suppress a session reading the body** *(filed 2026-08-23 from that skill's own review; tested and closed 2026-08-25)*. **Decision: keep the description exactly as it is — no rewrite.** Ran the item's own prescribed micro-test protocol before touching a character, rather than rewriting on a hunch: 10 fresh, fully independent subagents (5 given the real current description verbatim, 5 given a stripped no-guidance control with the capability-summary sentences removed, keeping only the trigger clause), each handed nothing but that description plus a realistic task ("remove the background from an icon that has a same-colour interior region that must stay opaque") and asked to state their exact command plan without executing anything. Every transcript read by hand, not scored by keyword count. **Result: 10 of 10 opened and read SKILL.md's actual body before answering, and all 10 independently derived the identical correct workflow** — `--recommend` alone (never `--analyze` first, the exact redundancy named elsewhere in this file), `--auto` as the render step, the coin-flip enclosure refusal answered with `--assume-protect` using the user's stated intent, `--protect-outline-color` preferred over `--protect-region` as a documented last resort, and `--verify` plus a manual visual check at the end. **Zero measurable difference between arms** — the control (summary removed) showed no more or less body-consultation than the treatment (summary present), so the mechanism `superpowers:writing-skills` warned about is not occurring here. This is exactly the outcome the original filing predicted as likely for a reference-shaped skill whose body a session must read anyway to get a real command line, and that skill's own instruction is explicit: if the control doesn't exhibit the failure, stop and close the item rather than author a fix for a problem that isn't happening. Frontmatter `name` and `description` both left byte-for-byte unchanged.

- **Re-analysing a GIF this tool wrote classifies antialiased art as pixel art (8.3%), and a detector for "this is our own output" was BUILT, MEASURED and FALSIFIED** *(filed 2026-08-18, decided 2026-08-19)*. **Decision: stays a documented hazard, do not fix.** The autonomy-shaped fix would be for the tool to recognise its own output and suppress the verdict, and there is a plausible signature: it dithers the alpha transition band with a fixed 8×8 Bayer matrix, which source art never does. Correlating the boundary band's transparent/opaque pattern against BAYER8's threshold ordering, over 30 third-party transparent GIFs versus 30 of our own renders: medians **+0.0010 vs −0.0006**, distributions almost entirely overlapping, and **1 of 30** of our own files clears a 0.02 threshold. The signature does not survive to the file — `--auto`'s edge-cleanup erosion trims the dithered ring and gifsicle re-quantizes the boundary on the way out. ⚠️ Three attempts were needed before the measurement meant anything, and the first two returned confident-looking answers: attempt 1 scored already-transparent sources against our output of them, where `--auto` correctly does nothing, so it compared each file with itself; attempt 2 used opaque sources, which have no alpha boundary, so it returned zero pairs. The standing guidance is unchanged and already in SKILL.md: **judge content type from the ORIGINAL, never from a file this tool has written.** `references/lessons.md` §29.9, §33.1.

<details><summary>Original item, kept verbatim</summary>

### `[P2 · M · Opus5-High]` Re-analysing ANY GIF this tool writes classifies antialiased art as pixel art — 8.3% with no compression at all *(filed 2026-08-18; retitled twice the same day as the sample grew 3 → 14 → 48)*

**Added 2026-08-18 by the v5.5.0 audit pass, asking what a new measure does to the tool's OWN output.** `--compress heavy` quantizes colours, and quantization gives a 1px antialiasing ramp flat neighbours it did not have — so `plateau_cliff_ratio`'s founding premise ("a 1px ramp cannot be a cliff, because the ramp pixel is a plateau of length 1 by construction") is a property of the FILE, not of the artwork. Measured, source → its own `--compress heavy` output: `gem.gif` 305 colours/not-hard-edged → 122/**hard-edged** (cliff 0.324); `heart_ORIGINAL.gif` 320/not-hard-edged → 108/**hard-edged** (cliff 0.343); `add.png` unchanged. Two of three flip. Reachable in an ordinary workflow — compress for a size limit, then run the tool again on the output — and it is the likeliest explanation of the two false positives that survive in the corpus, both already-processed cutouts caught by the cliff ratio.

**The obvious veto was measured and rejected, so do not re-derive it.** Over the 451 assets the cliff rule detects correctly, colour count is median 12 / p95 20 but reaches 50, 56, 98, 123, 172, 221, 242 and 1,233 — dithered and photographic pixel art is exactly what that rule exists to reach. A veto at >100 (needed to catch the 108/122 cases) removes **0 of 2** corpus false positives and costs 5 real detections; at >80 it removes 1 of 2 and costs 6. Not implemented.

⚠️ **2026-08-18 — MEASURED ON 48 ASSETS, and both this item's original title and its first correction were wrong.** Every asset run through the real CLI at all four tiers and re-analysed: **`none` (no compression, the control) flips 4/48 = 8.3%**, `optimize` **12/48 = 25.0%**, `medium` 10/48 = 20.8%, `heavy` 6/48 = 12.5%. **14 of 48 (29.2%) flip at some tier, and `heavy` is the LEAST affected.** Nearly every flip is `plateau_cliff_ratio` clearing 0.30 (0.303–0.573 over 2,150–16,104 steps).

**The control IS the finding: `--compress` is not required.** Writing a GIF is itself a palette reduction — `add.png` goes 402 composited colours to 133 with no compression flag — so the hazard belongs to the container, not to a tier. **The rule is: decide content type from the ORIGINAL asset, never from any file this tool has written.** ⚠️ Note the sampling failure worth carrying: a 14-asset PREFIX of an evenly-spread sample said the control flipped 0, which is how the previous correction got written. A prefix of a spread is not a spread. **Do:** the size of the class is now known; what remains is deciding between a real discriminator for quantization-induced cliffs (a dither signature is the obvious candidate — gifsicle's colour reduction leaves one and hand-drawn pixel art does not) and documenting it as a hard limit in SKILL.md's content-type section.

📏 **2026-08-18 — the class is also measured on 118 assets from the opposite direction, and the item stays OPEN because measuring it is not fixing it.** Real antialiased emoji downscaled to 64px and quantized to a small palette — which is what any lossy small-icon export IS — reach pixel art's numbers: at 32 colours 11.0% clear a palette-mass threshold the Orc sprites sit inside, at 16 colours 30.5%. **That reaches the 16-colour floor already in the released code too, not just the cliff ratio**, so this is a general property of quantization rather than a quirk of one measure (§29.12). **Do:** build a small corpus of the tool's own outputs at each `--compress` tier and score them, so the size of the class is known rather than guessed (3 assets is not a population). Then decide between a real discriminator for quantization-induced cliffs — a dither signature is the obvious candidate, since gifsicle's colour reduction leaves one and hand-drawn pixel art does not — and accepting it as documented. `references/lessons.md` §29.9

</details>

- **The 31 `unsuitable_no_edges` overlay plates lose 5–34% of their pixels to `--auto`, and it is NOT art loss** *(filed 2026-08-18 as a possible quiet-data-loss defect, resolved 2026-08-19)*. **Decision: accept, no guard.** The item rested on two plates rendering at "88% and 93% of source alpha — 240,218 and 139,720 opaque pixels gone". All 54 degenerate/ambiguous assets were then run through `--auto` (1 refusal, 53 outputs; median survival 94.5%, worst 26.4%) and the removed pixels inspected on the eight worst. **On every overlay plate, 100.0% of the removed pixels are background-coloured AND 100.0% are in the border-connected background component; interior artwork removed: 0 px.** The plates carry no source alpha at all (`1800x1200 = 2,160,000` fully opaque px), so "survival" there is not a loss ratio — it is simply the share of the canvas that was not background, and these are dark overlay effects on a large flat field. **The falsifier settles it:** a `survival < X%` warning would fire on **8–10 of 59 ordinary assets at every threshold from 70% to 99%** — real emoji and sprites at 16.5%, 18.2%, 26.4%, 31.5% — so survival cannot discriminate a defect from a background. `_refuse_empty_render` does work and is exercised: the flat 1-colour tile is the single refusal, exiting 1 and writing nothing. ⚠️ **One residual, recorded rather than guarded:** on `gradient_beds/diorie__bed-forest.png`, 449 of 2,737 removed px (16.4%) are NOT border-connected — a smooth alpha ramp reaching background-coloured values in an interior pocket. Small, confined to constructed gradient fixtures, and the wrong thing to build a threshold on. Revisit only with a real asset that loses visible interior art, not with a percentage.

<details><summary>Original item, kept verbatim</summary>

### `[P3 · S · Sonnet5-High]` The 31 `unsuitable_no_edges` sprites are excluded from RENDER baselines too, and `_refuse_empty_render` is untested *(filed 2026-08-18)*

`EXCLUDED_LABELS` exists so 31 flat overlay plates do not hand a hard-edge rule 31 free true negatives that test nothing. `render_baseline.py` inherits that exclusion by reusing `iter_assets`'s scoring default — which is an accident, not a decision: those files are exactly the degenerate inputs a RENDER gate wants. The flat 1-colour tile is a natural `_refuse_empty_render` test and has never been run through one. ✅ **Both halves measured 2026-08-18, and `include_excluded=True` is now passed in the render harness.** The refusal WORKS: the flat 64x64 1-colour tile hits `_refuse_empty_render`, exits 1, writes nothing, and names both usual causes plus the two `--analyze` fields to read. That is the guard's first run against the input it exists for.

⚠️ **What the same test found is why this item stays open at P3 rather than closing.** Two 1800x1200 overlay plates from the same excluded set render at **88% and 93% of their source alpha** — 240,218 and 139,720 opaque pixels gone — with no refusal, no warning, and `--auto` reporting success. They are labelled `unsuitable_no_edges` for SCORING purposes, but the tool does not refuse them, so a user pointing it at one gets quiet art loss. **Do:** decide whether that class should refuse, warn, or is acceptable, then run the full 31 through the render harness to size it. ⚠️ A sweep was started and ABANDONED at 12 of 31 — those plates are up to 3840x2160 and cost minutes each in `analyze()`, so budget for it rather than assuming it is quick; the three-asset probe above is what the numbers here rest on.

</details>

- **`--verify` on a missing/unreadable file surfaces a raw `FileNotFoundError`/ `PIL.UnidentifiedImageError` traceback**, found in the same 2026-08-07 whole-branch review. **Decision: not a defect, don't fix.** Confirmed every other file-reading path in `scripts/remove_gif_background.py` already behaves the same way (no defensive validation anywhere in the file) — adding it only to `--verify` would be a new inconsistency, not a fix, per this codebase's own established convention (trust internal/framework guarantees, validate only at a real system boundary the rest of the file already treats as one). Revisit only if this convention itself changes project-wide, not in isolation for this one flag.

---

## 🔔 Reminders / watch-for

### SUGGESTED BUNDLE — the small items worth doing in ONE session *(added 2026-08-18)*

Not a new item; a routing note, because the dominant cost of any of these is the release gates, not the work. **Four were identified as bundleable, three were done on 2026-08-18, and this records the one that was NOT plus what to bundle it with.**

**Done in that bundle:** the `audit_docs.py` marker gate (closed, archived) · the render harness's degenerate-asset exclusion (harness fixed; the item stays open on a NEW finding — two overlay plates lose 7–12% of alpha with no refusal) · APNG (halved — an independent decoder confirms 19 frames / 18 distinct; the browser half stays open).

**Not started, and the natural next bundle:**
1. **`--translucent-region` coverage** — pure test work, no code change expected. The `circle:` form, several `;`-joined specs, alpha at 0.0 and 1.0, and interaction with `--crop`/`--resize-max-dim`. One render plus one pixel assertion per case; `parse_protect_regions` and `apply_translucent_regions` both already exist.
2. **APNG's remaining browser half** — one file, one real browser, one look. ⚠️ Read §29.14 first: the in-app preview pane animates NOTHING, so a "static" result there is vacuous and a control is mandatory.
3. **The overlay-plate art loss** filed under the render-exclusion item — decide refuse / warn / accept, then size it with the full 31.

**Do NOT put these in that bundle, and the reason is not effort:** `--source-alpha-band`'s batch plumbing is XS but sequenced behind "is the 2px band earned at all" — plumbing it first risks building something the answer deletes. The frame-0/all-frames predicate asymmetry is a 3-line edit that triggers a full corpus re-score plus a render pass, because `_src_bg_transparent` gates a hardness rule. The outline-leak-gate fix touches candidate selection, which has already caused one regression here.

**The highest-leverage item is not code at all:** acquiring ~20 real 32–128px ANTIALIASED icons unblocks three of the four P2s at once, because every one of them fails for the same reason — this project owns essentially no antialiased art at sprite scale, so any measure that helps there scores perfectly and is untestable for false positives (§29.12).

### AUTONOMY BACKLOG — 11 of 13 CLOSED; items 5 and 13 remain *(opened 2026-08-17; counts corrected 2026-08-17, 2026-08-18, and again 2026-08-18 by enumeration)*
**Counted by enumerating the numbered items below, 2026-08-18 — the third correction to this line, and the first one derived rather than remembered.** 13 items; 11 struck through and closed; **open: 5** (gift's sparkle colour — the one genuinely unsolved research item) **and 13** (the narrow numeric constants still calibrated on one art family, marked PARTIALLY ADDRESSED). Measurements behind each closure: `references/lessons.md` §18, §21. ⚠️ **The three things this line got wrong, because they are the shape of error to expect here.** It read "8 of 9 CLOSED; only item 5 remains" while (a) there were 13 items, not 9 — five were appended after the heading was written and nobody re-counted; (b) 11 were closed, not 8; and (c) it named **item 9 as open** directly above item 9's own `✅ CLOSED 2026-08-17` marker. A heading that summarises a list it sits above will drift every time the list grows. **Re-derive the count from the items; never edit one number in this sentence and trust the rest.**

1. ~~`--pixel-art` emitted on thin-AA vector art~~ — **CLOSED.** Added a second discriminator, `antialiasing_blend_ratio` (are there real background-to-art blends at all?). Synthetic pixel art 0.000; the lowest real asset 1.530. `appears_hard_edged` now needs BOTH measures. The fixture still gets the flag; love and heart no longer do. §18.1
2. ~~`--erosion-exempt-max-size` emitted for love~~ — **CLOSED.** The classifier was already right (love's 4 buttons ARE detected as design); the SIZE THRESHOLD was the problem — 487px sat above the buttons' own 286–306px, so it would have exempted them anyway. Now suppressed whenever the transient and persistent size ranges overlap. §18.2
3. ~~heart's `--feather-band-multiplier 1.5` unverified~~ — **CLOSED, and it was actively harmful.** Measured fringe fraction 0.2186 at 1.5 and 0.1831 at 2.5 against 0.0000 at the default — the recommendation was producing the fringe. The `max(1.5, ...)` clamp was the bug. Only recommended at ≥3.0 now; `--protect-band-only` carries the rest (97.7% of the same protection, no fringe). §18.3
4. ~~gift's white strip never surfaced~~ — **CLOSED.** Not a colour-detection failure: the union-across-frames footprint had merged the strip with a transient pocket (21,184px → 25,219px), and nothing encloses a shape that never exists. Added per-frame re-verification. gift now auto-recommends `--protect-outline-color 002864`; protected coverage 0.0 → 0.874, fringe 0.0388 → 0.007. §18.4
5. **STILL OPEN — gift's sparkle colour never appears at full opacity.** ⚠️ **Its mechanism was MEASURED 2026-08-21 and is shared with the soft-glow-on-dark item above**: `detect_fading_colors`'s `partial_fraction > 0.9` gate asks that a fading colour be almost never solid, so any colour that is simultaneously solid artwork and a translucent element fails it by construction. The glow asset misses the gate by 0.00023. Work these two together — `references/lessons.md` §40. The curved sparkle is `#052a75`, the SAME navy as the box outline, drawn at partial opacity; one colour is simultaneously solid artwork and a translucent element, so marking it fading would stop the outline blocking anything. The 4-dot sparkle separately needs `--fade-color 6969f2` because its true colour never clears the palette frequency floor. **Do:** the per-pixel art prior built for the flood barrier (§16) is the plausible basis — high prior ⇒ outline, low prior ⇒ sparkle. ⚠️ Read the REVERTED "saturation promotion" experiment in §16 first; the naive version of this idea is measurably net-harmful. This is the one genuinely unsolved item and it is a research task, not a parameter fix.
6. ~~GIF `--target-kb` discards `--square-pad`~~ — **CLOSED: did not reproduce.** Measured 128×128 with and without `--crop`; padding survives the tier cascade. The original 128×110 came from a render script that never passed the flag to the GIF variant. §18.6
7. ~~`--verify`'s `looks_fringed` is unreliable~~ — **CLOSED.** Replaced with the outer-ring relative metric, and made TRI-STATE: True >0.15, False <0.04, None in between with the reason. The bands overlap across assets (heart's fringed 0.0665 < crystal's clean 0.0830), and tightening the ratio collapses every asset to 0.0000 — a test that cannot fail. Reporting "inconclusive" is the honest answer. §18.5
8. ~~**AVIF durations cannot be read back**~~ — **RESOLVED 2026-08-17.** Never an AVIF limitation: Pillow populates `info['duration']` only in `load()`, and the reader used seek-only, so every frame returned 0. Adding `load()` returns the true durations (3000 ms, `[220, 20 x122, 340]`). Same root cause as the WebP source-duration shift — see `references/lessons.md` §17.
9. ~~**`--verify` has no 8-bit-alpha mode**~~ — **CLOSED 2026-08-17.** *(was: partially addressed)* the fringe metric itself is now alpha-aware (`measure_outer_ring_background_fraction` counts only near-opaque pixels, so a legitimate alpha ramp is not flagged), and `--auto`'s post-render check runs on WebP/AVIF using it. `--verify` proper still refuses non-GIF because its OTHER checks (leftover background, protected coverage) remain 1-bit assumptions. **Do:** give those two the same partial-alpha treatment, then lift the refusal. (opened 2026-08-17, replaces the stale duration-based reason). Now that WebP/AVIF durations read correctly, the ONLY thing blocking `--verify` on those formats is that its checks are written for 1-bit alpha: "leftover background" tests for an opaque background-coloured pixel, "fringe" for a pale ring — and a recovered fade is legitimately both. **Do:** give each check a partial-alpha definition (leftover background = alpha≈255 AND background-coloured; fringe = the outer-opaque-ring metric from §16, which is already alpha-aware) rather than lifting the refusal. Lifting it without redefining the checks reintroduces exactly the misleading pass the refusal exists to prevent.

10. ~~**`--verify`'s leftover-background count flags intentionally protected design**~~ — **CLOSED 2026-08-17.** Now also excludes the verified outline's own filled area (gift worst frame 14,243 → 0) and requires alpha>=250 so a partly transparent background-coloured pixel counts as the fade/ramp it is. §21.2. Original note: (opened 2026-08-17). On gift, `leftover_background_opaque_px` reports 14,243px on its worst frame; 73% of them are inside the protected white-strip region's own bbox. The strip is background-COLOURED design that is *supposed* to stay opaque, so counting it as leftover background is a false alarm — visually confirmed correct output. `analyze()`'s own leak check agrees (`leaked_pixel_count: 0`). **Do:** exclude pixels inside a verified protected region's per-frame footprint from the leftover count, the same way `bbox_scope`-restricted enclosure already does for part of it. Low severity (a noisy metric, not a bad output) but it undermines trust in an autonomous run's self-check.

11. ~~**Replace the erosion-exempt SIZE threshold with per-region masks**~~ — **CLOSED 2026-08-17** via `--erosion-exempt-transient` + `find_transient_removed_regions()`, auto-recommended wherever the size threshold is refused. §21.4. Original note: (opened 2026-08-17 during the §20 audit — a better fix than the guard shipped in §18.2). The overlap guard picks the safer side of a conflict rather than dissolving it: when transient noise and design occupy the same size range, love gets no exemption at all, so the v3.1.0 small-region inflation bug can return. **The machinery to do it properly already exists** — `find_tiny_removed_regions` produces per-frame MASKS and `erode_alpha_edge_exempting_tiny_regions` consumes masks, so a size threshold is not actually required. **Do:** classify each region persistent-vs-transient (the classifier is already correct), build the exemption mask from the transient regions ONLY, and drop the size threshold entirely. That exempts exactly the noise regardless of how big they are or what size the design happens to be.

12. ~~**gift's protected-region coverage is 0.874, not 1.0**~~ — **CLOSED 2026-08-17: it was never real.** Coverage was measured over every background-coloured pixel in the bounding RECTANGLE (12,371 at frame 0) instead of the enclosed footprint (10,257); the extra pixels are genuine background, correctly transparent. Restricted to the footprint it reads 1.000, so item 4 is genuinely closed. §21.1. Original note: (opened 2026-08-17). §18.4 took it from 0.0, which is a real fix, but 12.6% of the region still is not opaque and I did not check why. It may be legitimate (frames where the strip is genuinely occluded) or a real remainder. **Do:** identify which frames and which pixels account for the gap before treating item 4 as fully closed. "Improved" was reported; "resolved" was not established.

13. **The whole threshold set rests on one art family** (opened 2026-08-17, §20.6). ⚠️ **PARTIALLY ADDRESSED 2026-08-18 — read this before repeating the claim.** The corpus is no longer one family: 31 labelled assets (25 pixel art / 6 antialiased, 20 of them on genuinely non-white opaque backgrounds) plus 145 vector emoji now used as a standing falsifier population. Three things have been re-scored against it — the edge-hardness measures split by background colour (§23.7), and two candidate pixel-art discriminators, both killed by the emoji set (§23.8, §23.9). What is STILL one family: the narrow numeric constants (fringe bands 0.04/0.15, floor tolerance 0.02, post-render margin 0.05), which remain calibrated on 4-5 flat vector icons on white and have not been re-measured on a dark or coloured background. Five flat vector icons on white plus a self-authored pixel-art fixture. No dark-background asset, no real-world pixel art, no photographic-ish source was tested. The blend-ratio margin (0.000 vs 1.530) is wide; the rest (feather ≥3.0, fringe bands 0.04/0.15, floor tolerance 0.02, post-render margin 0.05) are 4–5 points from one style. **Do:** re-measure against a genuinely different asset before trusting any of the narrow constants on new content — especially a non-white background, which the ring metric has never seen. ⚠️ **A FRESH INSTANCE, 2026-08-20:** the changing-background rule was validated on 71 OPAQUE animated assets and shipped refusing 9 of 37 already-background-removed ones — a false-positive class its control could not contain, caught only by a PRE/POST render diff. The thesis of this item is not historical; it reproduced on a brand-new rule the same week (`references/lessons.md` §36).

✅ **UNBLOCKED 2026-08-20 — the corpus now exists.** `local/corpus dark/` holds 119 files supplied, **all with opaque backgrounds**. **111 usable after filtering** — 2 byte-identical duplicates removed and 6 with no keyable background moved out. Re-measured on the final set: **53 dark** (<80 luminance), **55 saturated** (chroma ≥60), **65 animated**. That takes the corpus from **5 dark assets to ~60**, across two independent sources instead of one. ✅ The 12 non-keyable files were **moved to `local/corpus dark/_excluded/`** on 2026-08-20 with the measurement recorded — moved rather than name-listed, because a name list is a discipline and a moved file is a control. **111 remain.** Sequenced as **Task 8** of `docs/plans/2026-08-20-post-trial-defects.md`, registered as `ambiguous` so it is excluded from every recall and specificity figure: its job is the ring metric, not classification.

